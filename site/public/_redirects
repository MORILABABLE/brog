# Cloudflare Pages のリダイレクト設定。
# このファイルは public/ から dist/ の直下にコピーされ、Pages が読み取る。
# 書式は「元のパス  行き先  ステータス」。1行1つ。
#
# ★ カテゴリページを「ハブ」にまとめた（2026-08-26）。
#   「配信終了予定」と「終了済み」は /category/leaving の1ページに並べている。
#   /category/ended はもう生成されないので、ここで恒久転送する。
#   （まとめた理由は site/src/config.ts の CATEGORY_HUBS）
#
# ★ ハブの構成を変えたら、この表も必ず直すこと。
#   config.ts だけ直すと、生成されないURLが 404 のまま残る。
/category/ended  /category/leaving  301

# ★ 総合1本だった頃の配信終了記事。サービス別2本に置き換えたので退役させた
#   （2026-08-26）。旧記事の65作品は新しい2本が漏れなく引き継いでいる
#   （2026-09-leaving-netflix 36本 / 2026-09-leaving-prime-video 30本）。
#   行き先を片方の記事にすると、もう片方を読者が見つけられない。ハブへ送る。
/posts/2026-09-leaving  /category/leaving  301

# ★ 配信カレンダーを「サービス × 向き」の2枚にまとめた（2026-09-17）。
#   /leaving/<サービス> … 終了予定＋終了済み ／ /arrivals/<サービス> … 配信開始予定＋新着
#   （中身は site/src/components/ServiceCalendarPage.astro、理由は site/src/lib/evergreen.ts の冒頭）
#
# 1. 終了と新着を1枚にしていたカレンダー（noindex・サイト内の入口は2か所だけだった）。
#    役目は2枚のカレンダーが引き継いだ。終了側へ送る（ページ上のタブで新着側へ移れる）。
/calendar/netflix  /leaving/netflix  301
/calendar/prime-video  /leaving/prime-video  301
/calendar/disney-plus  /leaving/disney-plus  301

# 2. カテゴリ×サービスの記事一覧（/category/<ハブ>/<サービス>）は**転送しない**。
#    2026-09-17 に、カレンダーのある3社だけ /leaving/<サービス> へ送る6行をここに書いたが、
#    翌 2026-09-18 に取り消した（運用者の指定）。ヘッダーの「配信終了済み・予定」「新着配信」は
#    過去記事の一覧の入口で、カレンダーの入口ではない（site/src/lib/evergreen.ts の `hubServiceHref`）。
#    ★ 5サービスぶんのページを生成しているので、**ここに転送を書き足すと記事の一覧が読めなくなる。**
#    ★ 検索語の重なり（/category/leaving/netflix ↔ /leaving/netflix。2026-09-17 の実測）は
#      転送ではなく noindex で受けている（site/src/pages/category/[category]/[service].astro）。
