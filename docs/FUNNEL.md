# 読者がどこで落ちているか（GA4・Search Console の実測）

最終更新: 2026-09-06

**このドキュメントだけが「実際の読者の行動」を根拠にしている。**
[GROWTH.md](./GROWTH.md) は設計の理屈、[AFFILIATE.md](./AFFILIATE.md) は実装と規約。
ここは**測った数字**と、そこから見えた詰まりを置く。

数字はすべて `npm run analytics` の出力（2026-09-06 実行）。推測には「未検証」と明記する。

| 関連 | |
|---|---|
| 流入施策の設計と理屈 | [GROWTH.md](./GROWTH.md) |
| 収益の実装・提携・規約 | [AFFILIATE.md](./AFFILIATE.md) |
| どの検索語を狙うか | [KEYWORDS.md](./KEYWORDS.md) |
| 「いまどこで観られるか」の設計（保留中） | [CROSS-SERVICE.md](./CROSS-SERVICE.md) |
| 作品ページの実装 | [WORK-PAGES.md](./WORK-PAGES.md) |

---

## 0. 3行で

1. **面の89%（459/517の作品ページ）が「終了しました」で終わっている。** 検索の答えになっていない
2. **順位を揃えて比べると、作品ページのCTRは記事の 1/3.4。** 順位ではなくタイトルの問題
3. **90日で外部リンククリック8件（PVの3%台）。** 収益の入口が開いていない。流入より深刻な詰まりはこちら

### 進捗

| | 状態 |
|---|---|
| **7-1 作品ページの見出し**（1・2に効く） | ✅ **2026-09-06 実施**。「終了しました」0枚に。第2段（在庫取得）は未着手 |
| **7-1-3 記事テンプレートにも同じ直し** | ✅ **2026-09-06 実施**（テンプレと品質ゲートのみ。公開済み4本は次の書き直しで新形へ） |
| **7-2 常設ページのタイトル**（2に効く） | ✅ **2026-09-06 実施** |
| 7-3 シリーズ記事を増やす | 未着手 |
| 7-4 U-NEXT の導線 | **提携待ち**（[AFFILIATE 12-0](./AFFILIATE.md)）。器は完成済み |
| **7-5 枠別のクリック計測**（3に効く） | ✅ **2026-09-06 実装**。`npm run analytics` に枠別の行が出る |
| **7-6 Amazon の導線の分けかた** | ✅ **2026-09-06 実装**（Prime無料体験の枠を9ページに）。**両取りは1本では不可**と確定 |
| **8 計測の汚れ** | ✅ **2026-09-06 対処**。レポート側でホスト名を絞った。残りは GA4 の内部トラフィック除外だけ（効果は小さい・8-4） |

---

## 1. 測り方

```bash
npm run analytics                # 直近28日
npm run analytics -- --days 90   # 期間を変える
npm run analytics -- --write     # data/analytics.json に落とす（.gitignore 済み）
```

`pipeline/cli/analytics.ts`。Search Console と GA4 を**1本のファネル**に並べる。
既存の `npm run queries` とは口を分けてある（あちらは記事に焼き込む素材、こちらは判断材料）。

- 認証は `queries.ts` と同じサービスアカウント（`secrets/gsc.json`）
- **GA4 側にも権限が要る** — 管理 → プロパティのアクセス管理 → サービスアカウントを「閲覧者」で追加
- `.env` の `GA4_PROPERTY_ID`（管理 → プロパティの詳細 の数字）

> ★ **Search Console のデータは 2026-08-29 から**しか無い。プロパティの登録がその時期。
> 「90日」で引いても実データは6日ぶんしかない（2026-09-06 時点）。
> 期間を伸ばしても母数は増えないので、**日別の推移で見ること。**

---

## 2. いまの数字

検索は **2026-08-29 〜 09-03（6日）**、サイト内は **2026-08-29 〜 09-05（8日）**。
GSC は反映が2〜3日遅れるので、期間が揃わない。

### 2-1. 検索

| | |
|---|---|
| 表示回数 | **831**（139/日） |
| クリック | **45**（7.5/日） |
| CTR | 5.4% |
| 平均掲載順位 | 8.4 |

日別で伸びている。**方向は合っている。**

| 日 | 表示 | クリック |
|---|---|---|
| 08-29 | 56 | 2 |
| 08-30 | 76 | 6 |
| 08-31 | 175 | 8 |
| 09-01 | 179 | 8 |
| 09-02 | 160 | 10 |
| 09-03 | 185 | 11 |

**個別に返った検索語は46語・表示124件だけで、残る85%は匿名化されたロングテール。**
これは GROWTH 3-1 の狙い（作品名の集合を取る）が当たっている証拠で、
**面を増やす方針そのものは正しい。**

返ってきた46語も狙いどおりの形をしている。

```
スリザー 配信 / レザボア ドッグス 配信 / アメリカンビューティー 配信
オッド タクシー アマプラ いつまで / 天気の子 アマプラ いつまで
u-next ウルトラマン 配信終了 / 五等分の花嫁 netflix 配信終了
その 女 諜報 員 アレックス どこで 見れる
```

### 2-2. サイト内（GA4）

**本番ホスト（`mihoudairader.com` と `www.mihoudairader.com`）に絞った
2026-08-29 〜 09-05 の8日ぶん。** 絞り方と、なぜ `www` を残すのかは8節。

| | 8日ぶん | 1日あたり |
|---|---|---|
| セッション | 159 | 19.9 |
| ユーザー | 142 | 17.8 |
| PV | 212 | 26.5 |
| PV / セッション | **1.33** | |
| エンゲージメント率 | 55.3% | |
| **外部リンククリック** | **7件（PVの 3.3%）** | 0.9 |

流入経路（同期間）:

| 経路 | セッション | ユーザー | エンゲージメント率 | |
|---|---|---|---|---|
| **Organic Search** | 106 | **92（11.5人/日）** | 66.0% | Google + Yahoo! + Bing |
| Direct | 40 | 37 | **25.0%** | **ほぼ全部が `/` 着地。大半がボット**（8-3） |
| AI Assistant | 6 | 6 | 66.7% | ChatGPT / Perplexity / Copilot |
| Organic Social | 3 | 3 | 0% | |

外部リンククリックの行き先: `amazon.co.jp` 4 / `video.unext.jp` 2 / `netflix.com` 1
（90日で通算8件）。

> ★ **「1日10〜20人」の正体はこう。** 本番のユーザーは 17.8/日 だが、
> **そのうち検索から来ているのは 11.5人/日**。残りは Direct（8-3 のとおり
> 大半がボット）と AI アシスタント。
>
> ★ **目標（1日100人）は検索流入の 11.5人/日 で測る。** 総ユーザー数を使わない。
> `npm run analytics` が「検索流入（直近7日）」の1行を出すのはこのため。

---

## 3. 詰まり① — 面の89%が「終わった」ページになっている

> **この節は 2026-09-06 の「変更前」の記録。** 対処は 7-1（実施済み）。
> **数字を書き換えないこと** — 効果を測るときの基準値になる。

**`site/dist/works/*.html` の `<title>` を全部数えた結果。**

| タイトルの型 | 枚数 | 割合 |
|---|---|---|
| 「◯◯」の見放題配信は**終了しました** | **459** | **89%** |
| 「◯◯」（◯年）は**いつまで見られる？** | 58 | 11% |
| 合計 | 517 | |

さらに、その58枚の終了予定日を見ると:

| 終了予定日 | 枚数 | |
|---|---|---|
| 2026-08-09 〜 09-05 | **30** | **もう過ぎている**。「いつまで見られる？」と書いてあるのに答えが古い |
| 2026-09-06（当日） | 1 | |
| 2026-09-07 〜 09-29 | 27 | まだ有効 |

**517枚のうち、いま「まだ観られる・◯日まで」と言えるのは28枚（5.4%）。**

### 3-1. なぜこれが効くのか

読者が打っている語は「スリザー **配信**」「その女諜報員アレックス **どこで見れる**」——
**「観たい。どこで観られる？」**という意図。

これに対して検索結果に出ている見出しはこう:

```
×  「スリザー」の見放題配信は終了しました
```

**答えではあるが、読者が求めている答えではない。** クリックする理由が無い。

そして踏んだ先も同じことしか書いていない。実際のページ（`/works/5346`）はこうなっている:

| 位置 | 出しているもの |
|---|---|
| h1 | 「スリザー」の見放題配信は終了しました |
| 状態 | Amazon Prime Video で 2026年8月14日に見放題配信が終了しました |
| ボタン | **「Amazon Prime Videoで見る」** ← 直前に「終了した」と書いた相手への導線 |
| 他社 | U-NEXT / Hulu / DMM TV の**検索結果に飛ぶだけ**（あるかどうかは保証しない） |
| CTA | 「見放題での配信は終了していますが、レンタル・購入なら観られる場合があります」 |

**「じゃあ今どこで観られるのか」に、このページは一度も答えていない。**

### 3-2. 順位のせいではない。**タイトルのせい**である

「作品ページのCTRが低いのは順位が低いからでは」を潰すために、**順位帯を揃えて比べた。**

| 順位帯 | 種類 | ページ | 表示 | クリック | CTR |
|---|---|---|---|---|---|
| **4〜10位** | **記事** `/posts/*` | 13 | 233 | 23 | **9.9%** |
| **4〜10位** | **作品ページ** `/works/*` | 87 | 309 | 9 | **2.9%** |
| **4〜10位** | 常設一覧 `/leaving/*` `/arrivals/*` | 4 | 159 | 5 | 3.1% |
| 1〜3位 | 作品ページ | 8 | 13 | 4 | 30.8% |
| 11位以下 | 作品ページ | 40 | 90 | 0 | 0.0% |

**同じ順位帯で、作品ページは記事の 1/3.4 しかクリックされていない。**
順位の差では説明できない。差はタイトルが見せている答えの側にある。

そして 1〜3位に入った作品ページは **CTR 30.8%** を出している（8ページ・母数は小さい）。
**ページの型が悪いのではなく、いま出している答えが悪い。**

### 3-3. 放っておくと比率は悪化する

`expiring`（終了予定）は毎月 `removed`（終了済み）に変わる。
新しい終了予定が入ってこなければ、**「いつまで」ページは自動的に「終了しました」ページになる。**
いま 89% の側は、来月には 92%、その次は 95% になる。

★ **これは記事側の `/refresh` と同じ問題だが、作品ページには同じ仕組みが無い。**
記事は事実がずれたら書き直す運転があるのに、517枚の作品ページには無い。

---

## 4. 詰まり② — 表示の91%が「4〜10位」に溜まっている

> **4-1 の対処は 7-2（2026-09-06 実施済み）。** この節の数字も基準値なので残す。

| 順位帯 | ページ | 表示 | クリック | CTR |
|---|---|---|---|---|
| 1〜3位 | 9 | 16 | 5 | **31.3%** |
| **4〜10位** | **114** | **756** | **39** | **5.2%** |
| 11〜20位 | 34 | 79 | 1 | 1.3% |
| 21位以下 | 8 | 20 | 0 | 0.0% |

**表示の91%（756/831）が 4〜10位に居る。** ここを1〜3位に押し上げるだけで、
**新しい流入を1件も増やさずにクリックは約6倍になる**（5.2% → 31.3%）。

11位以下は 99件の表示に対してクリック1件。**2ページ目は無いのと同じ**なので、
ここの34ページは「表示がある」と数えないほうがいい。

### 4-1. 常設ページのタイトルが検索語から遠い

`/leaving/netflix` は**単体で表示115件（サイト最多）なのにクリック2件・CTR 1.7%**。
8.1位でこのCTRは、順位相応（3〜5%）を大きく下回っている。

```
現在:  【2026年9月1日時点】Netflixで配信終了予定の作品一覧
```

- 先頭12文字が日付。**スマホのSERPは30文字前後で切れる**ので、
  読者が最初に読むのが「2026年9月1日時点」になる
- 9月中旬に見れば「9月1日時点」は**古い情報に見える**。鮮度の表示が逆に働いている
- 読者が打っているのは「デスノート配信終了」「ネトフリ五等分の花嫁 配信終了」——
  **作品名＋配信終了**。一覧ページのタイトルにはその受け皿が無い

GROWTH 3-4 に「`<title>` を検索語に寄せる」と書いてあるが、**まだ実施されていない。**

---

## 5. 詰まり③ — 外部リンククリックが 3.3%

**ここが目標に対していちばん遠い。**

| | 90日（全ホスト） | 直近8日（本番のみ） |
|---|---|---|
| PV | 288 | 212 |
| 外部リンククリック | **8** | **7** |
| PVに対する率 | **2.8%** | **3.3%** |

内訳は `amazon.co.jp` 5 / `video.unext.jp` 2 / `netflix.com` 1（90日）。
**成果は0件**（[AFFILIATE.md](./AFFILIATE.md) の実績と一致）。

**流入が4倍に増えた直近8日でも、外部クリックは7件。率は動いていない。**
流入が増えれば自然に踏まれる、という形にはなっていない。

### 5-1. 3つの枠が、全部いちばん弱い行き先を指している

作品ページには Amazon への導線が3か所ある（`work` / `cta` / `bar` でIDは分けてある）。
**3つとも行き先が同じで、しかも Amazon の検索結果ページ。**

```
状態ボタン  → amazon.co.jp/s?k=スリザー&i=instant-video&tag=jetbike-work-22
本文CTA     → 同上（tag=jetbike-cta-22）
追従バー    → 同上（tag=jetbike-22）
```

- **見放題が終わった作品**を Amazon で検索させている。読者が探しているのは見放題
- 作品ページ（`/gp/video/detail/…`）ではなく**検索結果**なので、読者があと1回選ぶ必要がある
- 成立しても報酬はビデオのレンタル/購入紹介料。**Prime無料体験 500円/件には繋がらない**

枠を3つに増やしても、**3つとも同じ弱い行き先なら踏まれる数は増えない。** 実測がそう出ている。

### 5-2. U-NEXT の作品別リンクは、提携が通っても成果にならない

ガイドライン【5】が **LP限定**（[AFFILIATE.md 12-1](./AFFILIATE.md)）。
いま作品ページに出ている

```
https://video.unext.jp/freeword?query=スリザー   ← 「U-NEXTで探す」
```

は、提携が通ったあとも**成果が却下される形**。90日で2クリック出ているが、
**この2件は通っていても0円だった。**

★ **提携が通った日に、この形のリンクを増やしてはいけない。**
U-NEXT は単価1,320円で最有力なので、**LPへの導線を別に用意する**のが正しい。

### 5-3. 枠別の実測が取れていない

GA4 の `click` は拡張計測（`outbound: true`）で自動に取れているが、
**どの枠から出たのかは分からない**。Amazon 側でトラッキングIDを分けてあるが、
**成果が0なのでアソシエイトのレポートは何も返さない**（クリック数は成果が出るまで見えない）。

いま `data-affiliate="true"` は付いているので、**枠名を持ったカスタムイベントを1本足せば**
「追従バーは踏まれているのか」「記事末尾のCTAは到達されているのか」が分かる。

参考: **スクロール90%到達は 64件 / 288PV = 22%。**
**ページ下部のCTAは8割の読者に見られていない。**

---

## 6. 目標までの算数

目標は **1日100人・月3,000人・アフィリエイト収益 月1〜2万円**。

### 6-1. 流入

| | いま | 目標 | 倍率 |
|---|---|---|---|
| 検索クリック（GSC・Googleのみ） | 7.5/日 | 100/日 | **13.3倍** |
| 検索ユーザー（GA4・Yahoo!/Bing含む） | 11.5/日 | 100/日 | **8.7倍** |

以下は GSC 側（13.3倍）で計算する。**厳しいほうで見ておく。**

13.3倍は2つに分解できる。

| 分解 | |
|---|---|
| **CTR** 5.2%（4〜10位）→ 31.3%（1〜3位） | **6.0倍** |
| 残り（表示回数を増やす） | **2.2倍** |

**順位を上げるほうが、面を増やすより効率が良い。**
いま 114ページが 4〜10位に居て、そこに表示の91%がある。
**新しいページを作るより、そのページ群の答えを直すほうが早い。**

### 6-2. 収益

月3,000人 × 1.33 PV/セッション ≒ **月4,000PV**。

| | 必要な数 |
|---|---|
| 月1.5万円（U-NEXT 1,320円/件） | **11.4件** |
| クリック→成果 を 2% とすると（**未検証の仮定**） | **570クリック/月** |
| 月4,000PV に対する必要な外部クリック率 | **14.3%** |
| いまの外部クリック率 | **3.3%** |
| 必要な倍率 | **4.3倍** |

> ★ **「クリック→成果 2%」はこのサイトの実測ではない**（成果が0件なので出しようがない）。
> VOD無料体験の一般的なレンジ（1〜3%）から置いた仮定。
> `npm run afb` が成果を返すようになったら、**まずこの数字を実測に置き換えること。**
> 2% が 1% なら必要なクリックは倍になり、結論（クリック率を上げないと届かない）は強まる。

**流入13倍と、外部クリック率5倍。両方要る。片方だけでは届かない。**

### 6-3. ★ Amazon だけでは目標に届かない（計算で出る）

同じ 570クリック/月 を Amazon（Prime無料体験 500円/件）だけに流した場合、
成果率を 1.5% と置くと **8.6件 = 約4,300円/月**。

同じ570クリックを送ったとき、送り先で月収がこれだけ変わる。

| 送り先 | 単価 | 置いた成果率 | 月4,000PV・外部クリック14.3% のときの月収 |
|---|---|---|---|
| Amazon（Prime無料体験） | 500円 | 1.5% | **約4,300円** |
| U-NEXT（afb） | 1,320円 | 2% | **約1.5万円** |
| DMM TV（afb） | 1,691円 | 2% | 約1.9万円（[5-3 で当面見送りと判断済み](./AFFILIATE.md)） |

> ★ **Amazon だけ成果率を低く置いている**（1.5%）。無料体験の申込みが成果条件なのに、
> 送り先が**ビデオの検索結果**で、そこに「Prime に入る」導線が無いため。
> **どれも未検証の仮定。** 順位は変わらないが、絶対額は実測で置き換えること。

**目標1〜2万円は、U-NEXT の提携が通ることが前提になっている。**
これは営業の問題であって実装の問題ではない。
**通るまでの間に外部クリック率を上げておくこと**が、通った日の収益をそのまま決める。

AdSense を足しても計算は変わらない。月4,000PV × RPM 300〜600円 = **月1,200〜2,400円**。
**主役にはならない**（補助としては入れてよい）。

---

## 7. 施策（着手順）

**1つずつ出す。** 3-2 の切り分けができたのは順位帯を揃えて比べたからで、
同じ週に複数出すとその比較ができなくなる。

### 7-1. ★ 作品ページの答えを「終わった」から「いまどこで観られるか」に変える

> **✅ 第1段（見出し・文言）を 2026-09-06 に実施。** 実装は下の「やったこと」。
> **第2段（在庫の取得・API消費あり）は着手していない。** 効果を見てから決める（7-1-2）。

**流入と収益の両方に効く唯一の施策。ここが1番。**

これは [CROSS-SERVICE.md](./CROSS-SERVICE.md) で 2026-09-05 に**保留した施策そのもの**。
保留の理由（6-1「在庫が取れる4社のうち収益になるのは1社だけ」）は妥当だったが、
**そのときには無かった数字が2つ出た。**

| 保留時の前提 | 実測で分かったこと |
|---|---|
| 「収益にならない導線を厚くすることになる」 | **そもそもクリックが90日で8件しか無い。** 厚い/薄い以前の話 |
| 「読者の利益は本物だが、いま出せる形では半端」 | 半端でも、**いまは「答えていない」。** 比較対象が間違っていた |

そして保留の判断は**シリーズ記事の表**についてのものだった。
**作品ページ517枚に同じ判断を適用した記録は無い**（CROSS-SERVICE は記事の表が対象）。
ここは別途判断してよい。

やること:

```
×  「スリザー」の見放題配信は終了しました
○  「スリザー」はどこで見れる？Prime Videoの見放題は8月14日に終了
○  「スリザー」はU-NEXTで見放題（Prime Videoは8月14日に終了）  ← 在庫が取れた場合
```

- `/shows/{id}` または `/shows/search/filters` の `streamingOptions.jp[]` で在庫を取る
  （[CROSS-SERVICE 2-1](./CROSS-SERVICE.md)。**`/changes` とは別物で、「配信中」と書ける**）
- **全517枚を一度にやらない。** API無料枠は500/月（`data/api-usage.json` の実績は 8月101・9月26）
- **表示回数のある135ページから**。効果はそこに集中している（表示412件 = 全体の50%）

> ★ **「終了しました」と書くこと自体は正しい。** 誤情報を出さない原則
> （`works.ts` 冒頭「絶対に守ること」）は崩さない。変えるのは**順番**で、
> 「終了した」を先に言うか、「どこで観られるか」を先に言うか、という話。

#### 7-1-1. やったこと（2026-09-06・API消費0）

**文言を `site/src/lib/works.ts` の1か所に集めた。** 以前は見出しが
`pages/works/[id].astro` にあり、「`WorkState` の表と1対1で対応させること」と
注意書きで縛っていた。**状態は毎日の収集で勝手に動く**（leaving → passed → ended）ので、
人が対応を保つ形だと動いた日に静かにずれる。`WorkState` と同じファイルに置けば
状態を足したときに型エラーで気づく。

| 追加した関数（`works.ts`） | 役割 |
|---|---|
| `workHeadline(w)` | h1 と `<title>`。**問い＋観測した事実**の形 |
| `workDescription(w)` | meta description |
| `stateSentence(s)` | 本文の状態1行（`[id].astro` から移設） |
| `serviceCtaLabel(s, url)` | 配信状況の行のボタン文言。**リンク先から決める** |

**見出しは状態ごとに4つ。** 状態が動けば見出しも自動で移る。

| 状態 | 枚数 | 見出し |
|---|---|---|
| `leaving`（終了予定・未来） | **28** | 「◯◯」は**いつまで見られる**？Netflixは9月29日に見放題終了 |
| `passed`（予定日を経過） | **30** | 「◯◯」は**まだ見られる**？Prime Videoの見放題終了予定日は9月1日 |
| `ended`（終了済み） | **459** | 「◯◯」は**どこで見れる**？Prime Videoの見放題は8月14日に終了 |
| `started`（開始を観測） | 0 | 「◯◯」は**どこで見れる**？Netflixが9月3日に見放題配信を開始 |

**「終了しました」で終わるページは0枚になった**（変更前 459枚）。

★ **`passed` に専用の見出しを与えたのが効く。** 変更前はこの30枚が
`leaving` と同じ「いつまで見られる？」を名乗り、**日付だけが過去**という
状態になっていた（3節の「答えが古い」30枚がこれ）。

★ **日付は「9月29日」形式**（`headlineDate()`）。年をまたぐ日付のときだけ年を残す。
スマホの検索結果は全角30文字前後で切れるので、年の5文字で問いが押し出される。
本文（`stateSentence()`）は年を落とさない — あちらは切れないので正確さを優先する。

#### 7-1-2. ついでに直した — ボタンの文言がリンク先と食い違っていた

**実測で 517枚中248枚（48%）**が、こうなっていた。

```
表示: 「Netflixで見る」   リンク先: https://www.amazon.co.jp/s?k=…&tag=jetbike-work-22
```

`work-links.ts` の `resolveUrl()` は**見放題が終わった作品を Amazon の検索に落とす**
（作品ページは生きているが、もう見放題ではないため）。それは正しい判断だが、
ボタンの文言は**状態から**組んでいたので、サービス名だけが元のまま残っていた。
`rel="sponsored"` の広告リンクが、行き先と違う名前を名乗っていたことになる。

`serviceCtaLabel()` が**URL を受け取って**決める形にした。実測で**517枚すべて一致**。

| リンク先 | 文言 | 枚数 |
|---|---|---|
| Amazon（見放題が終わっている） | Prime Videoでレンタル・購入を探す | 480 |
| Netflix の作品ページ | Netflixで見る | 37 |

#### 7-1-3. 同じ直しを記事テンプレートにも入れた（2026-09-06）

**作品ページと同じ形が記事タイプにも残っていた。** 全作が終わっているシリーズ記事の
タイトルが「◯◯シリーズの見放題配信が終了した作品32本」で、
作品ページの「終了しました」とまったく同じ構造になっている。

    ✕ 【保存版】「名探偵コナン」劇場版シリーズの見放題配信が終了した作品32本
    ○ 【保存版】「名探偵コナン」劇場版シリーズはどこで見れる？見放題配信が終了した作品32本

**変えたのはテンプレートと品質ゲートだけ。公開済みの4本は書き換えていない**
（conan-movies / transformers / ultraman / mission-impossible）。
次に `/refresh` が走ったときに新形になる。

★ **記事は実測でサイト最良の CTR（9.9%）を出している面。**
公開中のものを一括で書き換えず、**作品ページの結果を見てから広げるかを決める。**

★ **サービス軸の月次記事（`ended`）には置かない。** 「Disney+はどこで見れる？」は
文として成立しないし、76本の行き先が並ぶ月次記事は作品ページのような行き止まりにならない。
`/posts/2026-08-ended` の 21表示・0クリックは **10.1位**という順位の問題（4節）。
判断は `article-types/ended.ts` の `verifyTitle` にコメントで残してある。

| 変えた場所 | |
|---|---|
| 決まり（プロンプトに入る） | `templates/naming.md`「終わった記事は問いから入る」 |
| リードの固定文言 | `templates/fixed-phrases.md`（`ended-lead-closer` ほか） |
| 検査の共通の口 | `article-types/shared.ts` の `TitleRule.questionClause` |
| シリーズ／特報 | `article-types/series.ts` / `special.ts` |
| **置かないと決めた面** | `article-types/ended.ts`（コメント） |

根拠と経緯は [ARTICLE-RULES.md 5節](./ARTICLE-RULES.md)。

#### 7-1-4. まだやっていない（第2段）

- `/shows/{id}` `/shows/search/filters` の `streamingOptions.jp[]` で**在庫**を取り、
  「いまどこで観られるか」を裏付きで書く（[CROSS-SERVICE 2-1](./CROSS-SERVICE.md)）
- **表示回数のある135ページから。** API無料枠は500/月
- **見出しの効果（3-2 の CTR 2.9%）が動くかを2〜3週間見てから決める**

### 7-2. 常設ページの `<title>` を検索語に寄せる

> **✅ 2026-09-06 実施。** 実装は `site/src/lib/evergreen.ts` の `evergreenTitle()`。

```
×  【2026年9月1日時点】Netflixで配信終了予定の作品一覧
○  Netflixで見放題配信が終了する作品一覧【9月1日更新】
```

| 変えたこと | なぜ |
|---|---|
| 日付を**頭から末尾へ** | スマホの検索結果は全角30文字前後で切れる。頭に日付があると、読者が最初に読むのが「2026年9月1日時点」になり、**サービス名も「終了」も押し出される** |
| **「時点」→「更新」** | 9月中旬に「9月1日時点」を見ると古い情報に見える。同じ日付でも「更新」は手入れの新しさを名乗る。左の枠（`evergreenStamp`）が最初から「更新」だったので、そちらへ揃えた |
| 年を落として `【9月1日更新】` | 常設ページの基準日は必ず直近の収集日で、年をまたがない |
| **「見放題」を入れた** | レンタル・購入と区別する言葉が、この一覧のタイトルには無かった（記事タイトルの決まりと同じ理由。`templates/naming.md`） |

**素のタイトルも `evergreen.ts` の1か所に集めた**（`evergreenTitleBase()`）。
以前は `EVERGREEN_PAGES` と `pages/leaving/[service].astro` の**両方**に同じ文字列があり、
片方だけ直すと**同じページがカードと見出しで別の名前を名乗る**状態だった。

実際に出ているタイトル:

```
Netflixで見放題配信が終了する作品一覧【9月1日更新】
Amazon Prime Videoで見放題配信が終了する作品一覧【9月4日更新】
Netflixで見放題になった作品一覧【9月4日更新】
```

★ **`/leaving/netflix` 単体で表示115件（サイト最多）。1ページで効果が測れる。**

### 7-3. シリーズ記事を増やす

**実測で最も勝っている型。** `/posts/harry-potter` 単体で 93表示・12クリック・**CTR 12.9%**
——**サイト全クリックの27%が1本の記事から出ている。**

いまシリーズ記事は6本（harry-potter / ultraman / conan-movies / mission-impossible /
transformers / resident-evil）。素材は作品ページ517枚と `data/ledger.json` に既にある。

★ **「配信中」型を優先する**（`resident-evil` の型）。
「終了した」型（conan-movies / transformers / ultraman）は 3-1 と同じ問題を抱えていて、
**読者が次に取れる行動が無い。**

### 7-4. U-NEXT の導線（提携が通った日に）

**器はもう正しく作られている。** `site/src/lib/unext-ad.ts` は
ジャンル別LP・枠別の `id1`・ガイドラインの文言制約まで実装済みで、
**「freeword リンクは成果にならない」ことも同ファイルに明記されている。**
`.env` にリンクコードを入れれば496ページに出る（[AFFILIATE.md 12-2・12-3](./AFFILIATE.md)）。

**残っている判断は1つだけ。**

| | いまの状態 | 判断 |
|---|---|---|
| LP導線（`unext-ad.ts`） | 実装済み・未設定 | 提携が通ったら `.env` に入れる。**迷う余地は無い** |
| 「U-NEXTで探す」（`freeword` リンク） | 517枚の作品ページに出ている | **読者には有益・収益は0。残すか消すか** |

#### freeword リンクをどうするか（2026-09-06 の判断）

**問い1: `freeword` リンクを U-NEXT の収益に繋げられるか → 不可能。**

afb のガイドライン【5】が「下記で紹介しているLP以外での成果は却下」。
リンク先を差し替える技術的な手があるかどうか以前に、**規約で閉じている。**
`site/src/lib/unext-ad.ts` の `LP` の注意書きに同じことが書いてある。

**問い2: 削除して afb のリンクに置き換えるか → 置き換えない。**

実測で、**90日の外部クリック8件のうち2件が freeword**（25%）。
Amazon の5件に次いでサイトで2番目に踏まれている導線で、
**しかも「この作品を観たい」という意図が最もはっきりしている場所。**

消すと2つ失う。

| 失うもの | |
|---|---|
| 読者の利益 | 「じゃあどこで観られるのか」に対して当サイトが渡せる数少ない次の一手 |
| **信号** | どの作品でU-NEXTへ向かう意図が出ているかが分からなくなる。提携が通った日に、**どこにLPを置けばいいかを決める根拠が消える** |

**決定: freeword は残す。役割を分ける。**

| | 役割 | 成果 |
|---|---|---|
| 「U-NEXTで探す」（`freeword`） | **読者のための検索リンク** | 0円でよい |
| LP（`unext-ad.ts`） | **PR枠** | 1,320円/件 |

★ **並べても 5節の「選択肢を増やすな」に反しない。** あれは
「同じ用（どこで観るか）に選択肢を2つ出すな」という話で、
こちらは**用が違う**（この作品を探す／サービスに入る）。

#### 問い3: 従来のリンクの位置に afb のポップアップを出すか → **出さない**

理由は4つ。**どれか1つでも十分だが、4つとも当たっている。**

| | |
|---|---|
| **検索順位** | Google は**本文を覆う挿入（intrusive interstitial）を順位で不利に扱う。** いまサイトの生命線は検索流入（9.3人/日）で、そこを削る施策は本末転倒 |
| **AdSense** | 審査中のサイトでインタースティシャルは不利に働く。10節の判断（この枠は AdSense を優先する）と衝突する |
| **JSゼロ方針** | サイトのJSは `WorkSearch` と追従枠のトグルだけに閉じてある（[GROWTH 3-6](./GROWTH.md)）。ポップアップは開閉と再表示抑止の状態管理が要る |
| **afb 側** | リンクコードの改変が許されているのは `rel="sponsored"` までと確認済み（11-3）。包むこと自体は改変ではないが、**誤クリック誘導**と見なされる余地を残す |

**代わりにやったこと（2026-09-06）: 位置を直した。**

`pages/works/[id].astro` の `UnextCta` には
「**『他のサービスで探す』のすぐ後ろに置く**」と注意書きがあるのに、
**実際は間に `AmazonCta` が挟まっていた。** 注意書きどおりの順に直した。

```
変更前   他のサービスで探す → AmazonCta → UnextCta
変更後   他のサービスで探す → UnextCta → AmazonCta
```

**成果にならない `find` のクリックの、すぐ隣に成果になる導線を置く。**
ポップアップで割り込まなくても、**意図が最も高い位置に置けば同じ仕事をする。**
単価も U-NEXT 1,320円 に対して Amazon は数十円（[AFFILIATE 3-0-1](./AFFILIATE.md)）なので、
順番としてもこちらが先。

#### 提携が通った日に確かめること

1. afb のリンクコードが**固定LPだけ**か、リンク先指定パラメータを持つか
   （持っていても【5】があるので使わない。**確認するのは「使えない」ことの裏取り**）
2. ジャンル別LPが `unext-ad.ts` の `LP` の6種類と対応するか
3. `id1`（枠名）が成果データの `keyword` に返るか — `npm run afb` で確認
4. **freeword リンクのクリックが、LPのクリックに置き換わっていないか。**
   両方の合計が増えていれば成功。片方が減っただけなら並べ方が悪い

★ **作品ページに「U-NEXT に在庫があるか」で出し分けてはいけない。**
[GROWTH 2-3](./GROWTH.md) が「作品ページに U-NEXT 由来の情報を持ち込まない」と
決めている（台帳の写しに近づくため）。**LPのバナーは在庫と無関係に出す**か、
**U-NEXT 記事の側に置く**。この線は収益の都合で動かさない。

### 7-5. 枠別のクリック計測

> **✅ 2026-09-06 実装。** `npm run analytics` の「■ 枠別のアフィリエイトクリック」に出る。

**イベント名に枠名を入れてある**（`aff_cta` `aff_work` …）。

★ **なぜパラメータではなく名前なのか。** `gtag('event','affiliate_click',{slot:'cta'})` と
パラメータで送ると、**GA4 の管理画面でカスタムディメンションに登録するまで
レポートに出ない。** イベント名なら標準の `eventName` でそのまま読めるので、
**管理画面の作業なしで `npm run analytics` から見える。**

仕組みは `layouts/BaseLayout.astro` の**リスナー1本だけ**（イベント委譲）。
枠ごとにスクリプトを増やさない（JSゼロ方針。[GROWTH 3-6](./GROWTH.md)）。

| 枠 | どこ | 出している場所 |
|---|---|---|
| `work` | 作品ページの状態行のボタン | `pages/works/[id].astro` |
| **`find`** | **作品ページ「他のサービスで探す」** | 同上。**U-NEXT の検索リンクがここ** |
| `cta` | 本文のCTA | `components/AmazonCta.astro` |
| `prime` | プライム無料体験 | `components/PrimeCta.astro` |
| `unext` | U-NEXT の afb 枠 | `components/UnextCta.astro` |
| `bar` / `rail` | 画面下／右の追従枠 | `components/FollowBar.astro` / `FollowRail.astro` |
| `table` | 表の作品名リンク | `components/WorkTable.astro` |
| `poster` / `body` | 記事本文 | `plugins/rehype-affiliate.ts` |

実測（ビルド後の全725ページ）: `find` 1,551 / `rail` 725 / `bar` 691 / `cta` 682 /
`work` 557 / `table` 557 / `prime` 9。

★ **`data-slot` であって `slot` ではない。** Astro では `slot` が予約語で、
props に `slot` を渡すと**そのコンポーネントごと出力から消える**
（`components/UnextCta.astro` の注意書きと同じ罠）。

★ **拡張計測の `click` とは別に数える。** あちらは外部リンク全部の合計。
`data-slot` の無いリンクはこちらに出ないので、**合計がずれていてよい。**

### 7-6. Amazon の導線 — 「両取り」は1本のリンクではできない（2026-09-06 確認）

**Amazon 公式で確定した。**（[アソシエイト・プログラム紹介料率表 別紙](https://affiliate.amazon.co.jp/help/node/topic/GRXPHT8U84RAYDXZ)）

> Amazonプライムメンバー紹介は `https://www.amazon.co.jp/amazonprime?tag=…` の
> **リンクを経由した場合のみ**紹介料の支払対象となります

| 成果 | 取り方 |
|---|---|
| 商品の購入（ビデオのレンタル・購入、DVD・ブルーレイ、その他） | どのタグ付きリンクでもよい。**24時間クッキーが拾う** |
| **Prime無料体験 500円** | **専用リンクを踏ませないと0円。** クッキーでは出ない |

**つまり「VIDEO導線＋ストア導線の両取り」を1本でやることはできない。** 導線が2本要る。

#### 7-6-1. ★ ただし、置き場所が無い

無料体験LPは「**この作品はPrime Videoで見放題**」と言えるページにしか置けない。
終了した作品のページで「Prime会員になろう」は文脈が繋がらず、誤誘導に近い。

**そして実測すると、その置き場所が作品ページには存在しない。**

| 作品ページ517枚の内訳 | 枚数 |
|---|---|
| Amazon Prime Video × **終了済み** | 211 |
| Netflix × 終了済み | 188 |
| Disney+ × 終了済み | 59 |
| **Netflix × 終了予定（まだ観られる）** | **28** |
| Amazon Prime Video × 終了予定日を経過 | 21 |
| **Amazon Prime Video × 終了予定（まだ観られる）** | **0** |

**「まだ観られる」28枚は全部 Netflix。Netflix には提携先が無い。**
Prime Video で「まだ見放題」の作品ページは**0枚**。

無料体験LPが成立するのは、いま見放題の作品が並んでいる面だけ:

- `/leaving/prime-video`（終了予定の作品が並ぶ＝まだ観られる）
- `/arrivals/prime-video`（見放題になったばかり）
- Prime Video の月次記事（5〜6本）

#### 7-6-2. 終了作品459枚は、Amazonでは収益化できない面

物販の紹介料は**数十円のオーダー**（DVD&ブルーレイ 2.00%。
デジタルビデオの率は公式ページ2種で食い違うので断定しない。[AFFILIATE 3-0-1](./AFFILIATE.md)）。
**U-NEXT の 1,320円 とは2桁違う。**

**この459枚で狙うべきは U-NEXT のLP**（提携後）。
Amazon のリンクは**読者のために残す**が、収益源として数えない。

★ **U-NEXT のLPは在庫と無関係に出す。** [GROWTH 2-3](./GROWTH.md) が
「作品ページに U-NEXT 由来の情報を持ち込まない」と決めている。
`unext-ad.ts` は既にその形（在庫を見ずにジャンルでLPを選ぶ）で作られている。

#### 7-6-3. 枠は増やさない。役割を分けるだけ

作品ページの Amazon 導線は3つあって、**3つとも同じ検索結果に飛んでいる**。
増やすと Amazon の審査基準（「サイトの主目的がアフィリエイトリンク」）に近づく。

| 枠 | いま | 案 |
|---|---|---|
| `work`（状態行） | Amazon検索 | **そのまま**。読者の一次意図はこの作品 |
| `cta`（本文） | Amazon検索 | **面によって変える** — 下の表 |
| `bar`（追従） | Amazon検索 | `work` と同じ行き先に揃える |

| 面 | 枚数 | `cta` の行き先 | 状態 |
|---|---|---|---|
| `/leaving/prime-video` `/arrivals/prime-video` | 2 | **Prime無料体験LP** | ✅ 2026-09-06 |
| Prime Video の記事 | 7 | **Prime無料体験LP** | ✅ 2026-09-06 |
| 終了作品のページ | 459 | **U-NEXT LP**（提携後） | 器は完成・LP待ち |
| Netflix・Disney+ の面 | 多数 | 現状のまま（提携先が無い） | — |

#### 7-6-5. Prime無料体験の枠（2026-09-06 実装）

**判断も文言も `site/src/lib/prime-ad.ts` の1か所。** `components/PrimeCta.astro` は描くだけ。
**`AmazonCta` を置き換える**形にしてあり、並べて足していない。

実測で**9ページ**に出た。狙いどおりの面だけに出ている。

```
/arrivals/prime-video            /leaving/prime-video
posts/2026-08-arrivals-prime-video
posts/2026-09-arrivals-prime-video
posts/2026-09-leaving-prime-video
posts/2026-09-upcoming-prime-video-{anime,japanese,western}
posts/resident-evil              （「バイオハザード」＝見放題配信中のシリーズ記事）
```

**出さない面**: 終了済みの記事・作品ページ517枚すべて・ジャンル軸の横断記事・
Netflix / Disney+ の面。

★ **文言は「当サイトが観測したこと」しか言わない。**
「このページの作品は Prime Video の見放題です」と書きたくなるが、
**それは在庫の断定**（`works.ts`「絶対に守ること」）。
8月の新着記事を11月に読んだ読者に「見放題です」と言えば嘘になる。

```
1文目  …見放題配信が始まった／始まる予定／終了予定日が公表されている  ← 観測した事実
2文目  見放題の作品は、Amazonプライム会員なら追加料金なしで観られます   ← 一般の事実
```

訴求しているのは**会員特典であって、個別の作品の在庫ではない。**

★ **「今なら」「◯日間無料」を書かない。** 条件も期間も Amazon が決める。
書いた日に正しくても、変わった日に嘘になる（`unext-ad.ts` の COPY と同じ規律）。

★ **トラッキングIDは別に作ること**（`PUBLIC_AMAZON_TAG_PRIME`）。
他の枠は紹介料（購入額の数%）だが、これは固定報酬 500円/件。
**同じIDに混ぜるとレポートで単価が読めなくなる。**

#### 7-6-4. ★ ただし、これは2〜3倍の話でしかない

**90日で Amazon のクリックは5件。** 行き先を最適化しても、
**5件が3倍になっても15件**で、成果はほぼ0のまま。

| | 効き目 |
|---|---|
| 行き先を分ける（7-6-3） | **2〜3倍** |
| クリック数を増やす（3.3% → 14%） | **4倍以上**、しかも上の倍率と掛け算になる |

**順序としては 7-5（枠別のクリック計測）が先。**
いまは3枠のうちどれが踏まれているのかすら分かっていない。
**どの枠が踏まれているかを知らないまま、枠の役割を組み替えても当てずっぽうになる。**

---

## 8. 計測の汚れ（2026-09-06 に調べて対処）

> **調べた結果、7節を書いた時点の想定は2つとも外れていた。**
> 対処もそれに合わせて変えてある。**元の想定を残してあるのは、
> 同じ推測をやり直さないため。**

### 8-0. やったこと — レポートの側でホスト名を絞った

`npm run analytics` が **GA4 のすべての問い合わせに本番ホストの条件を掛ける**
ようにした（`pipeline/cli/analytics.ts` の `productionHosts()`）。
既定は `GSC_SITE_URL` のホストとその `www` 版。`.env` の `GA4_HOSTS` で上書きできる。

**外した内訳も必ず出す。** 隠すと「外した判断そのもの」が検証できなくなる。

```
■ ホスト名の内訳（上の集計に入れたもの／外したもの）
  入    111セッション   141PV  エンゲージ  49.5%  mihoudairader.com
  入     20セッション    23PV  エンゲージ  40.0%  www.mihoudairader.com
  外     10セッション    42PV  エンゲージ  60.0%  localhost
  外      8セッション     8PV  エンゲージ   0.0%  mitokou.com
```

### 8-1. `mitokou.com` — 別サイトではなかった

**想定（外れ）**: 別ドメインで同じ測定IDが動いている。測定IDを分ける。

**実際**（2026-09-06 実測）:

| 調べたこと | 結果 |
|---|---|
| DNS | **Cloudflare を指していない**。このサイトを配信していない |
| HTTP | 応答なし |
| GA4 の8セッション | **全部**が「アメリカ・(direct)・着地は `/`・1PV・エンゲージ0%」 |

`mitokou.com` は旧サイト名（観とこう）の候補ドメイン（[DEPLOY.md](../DEPLOY.md) の改名の記録）。
**分けるべき「別サイト」は存在しない。**

**測定IDはHTMLに書いてあるので、誰でも計測エンドポイントに直接送れる。**
送られたヒットは**サイトのJSを一度も実行していない**ので、

- `BaseLayout.astro` の `ga-disable` は効かない（そのコードが走っていない）
- GA4 の内部トラフィック除外も効かない（IPの話ではない）
- GA4 のデータフィルタに**ホスト名の条件は無い**

**つまり GA4 側では止められない。レポートの側で外すしかない。** それが 8-0。

### 8-2. `www.` — すでに301されていた。しかも**本物の読者**だった

**想定（外れ）**: リダイレクトされていない。Cloudflare で apex へ 301 する。

**実際**: 4つのパスで確かめたところ、**全部 301 で apex に飛んでいた。**

```
https://www.mihoudairader.com/                     → 301 → https://mihoudairader.com/
https://www.mihoudairader.com/works/2036           → 301 → …/works/2036
https://www.mihoudairader.com/leaving/prime-video  → 301 → …/leaving/prime-video
https://www.mihoudairader.com/posts/2026-08-ended  → 301 → …/posts/2026-08-ended
```

そして GA4 に `www` で記録されている20セッションは**本物の読者**だった。

| | |
|---|---|
| 国 | 日本 |
| 流入元 | google / yahoo / t.co / chatgpt.com |
| 着地 | `/works/2036` `/leaving/prime-video` `/posts/2026-08-ended` など実在ページ |
| エンゲージメント率 | 40% |

★ **`www` を除外してはいけない。** 外すと**実在の読者を20セッション捨てる**ことになる。
2-2 の数字が一度 146セッションになっていたのはこの取りこぼしで、
正しくは 159セッション。`productionHosts()` が `www` 版を既定に含めているのはこのため。

> ★ なぜ 301 されているのに `www` で記録されるのかは**分かっていない**（未検証）。
> ただし**行動は決まる** — 実データが本物である以上、含める。

### 8-3. Direct 40セッション — 運営者ではなく、ほぼボット

**想定（外れ）**: 運営者自身の確認とボット。GA4 の内部トラフィック除外（IP）で消す。

**実際**（90日・本番ホストのみ）: 着地はほぼ全部 `/`、そして**国が海外に散っている**。

| 国 | セッション | エンゲージメント率 |
|---|---|---|
| アメリカ | 20 | **0%** |
| (not set) | 7 | 0% |
| 中国 | 4 | 75% |
| フランス | 4 | 0〜100% |
| カナダ・台湾 | 各2 | 0% |
| ドイツ・韓国・イギリス | 各1 | — |
| **日本** | **6** | 50%前後 |

**IPの内部トラフィック除外では消えない**（国内からのアクセスではないため）。
運営者自身とみられるのは日本の数セッションだけで、**除外しても分母はほとんど動かない。**

**対処: 目標を `Organic Search` で測る。**
検索流入は着地も国も検索語も筋が通っていて汚れていない。
目標（1日100人）はもともと検索流入の話なので、そこで判断すればよい。
`npm run analytics` が

```
  → **検索流入（直近7日）: 65人 = 9.3人/日。** 目標（100人/日）はこの数字で見る
```

の1行を出すようにした。**総セッション数を目標に使わないこと。**

### 8-4. 残っている手作業（小さい）

**GA4 の内部トラフィック除外**（管理 → データストリーム → タグ設定 →
内部トラフィックの定義 → 自宅のIPを追加、そして 管理 → データフィルタ で有効化）。

8-3 のとおり**効果は小さい**（日本の数セッションぶん）が、
今後アクセスが増えたときに運営者の確認が混ざり続けるのは避けたい。
**急がない。7-1・7-2 の効果を読む妨げにはならない。**

★ `localhost` の除外は **2026-08-28 にすでに効いている**（`BaseLayout.astro` の
`ga-disable`）。前後で切って確認済み — 除外後の `localhost` は0セッション。

---

## 9. やらないと決めたこと

| | 理由 |
|---|---|
| **SNS・動画への投稿を増やす** | 実測で `t.co` 5セッション・エンゲージ20%。**自動化できる余地が無い**まま手を掛ける対象ではない。検索が伸びている今は検索に寄せる |
| **薄い作品ページを追加で出す**（第2弾の67枚） | 3-1 の問題を抱えたまま枚数を増やすと、比率が悪化するだけ。**7-1 を先にやる** |
| **人物ページの下限を 5作品 → それ以下に緩める** | 索引対象31枚のうち**表示が付いたのは2枚・計2件・クリック0**。広げる根拠がまだ無い（`people.ts` の `INDEX_MIN_WORKS`） |
| **AdSense を収益の主役に置く** | 6-3。月4,000PV では1,200〜2,400円。目標に届かない |
| **DMM TV のアフィリエイト化** | [AFFILIATE 5-3](./AFFILIATE.md) の判断（AdSense審査まで）を維持 |
| **Amazon のリンクをさらに増やす** | 5-1。**3枠すべてが同じ弱い行き先**なのが問題で、枠の数ではない |

---

## 10. 次に見る数字

**施策を1つ出したら、次を出す前にここを見る。**

**7-1・7-2 を出したのが 2026-09-06。** Search Console が見出しの変更を拾い直すのに
2〜3週間かかるので、**次に読むのは 2026-09-27 ごろ。** それより前の数字で判断しない。

| 見るもの | どこで | 何が起きれば成功か |
|---|---|---|
| 1. 作品ページの **4〜10位帯のCTR** | `npm run analytics` の「順位帯 × ページの種類」 | 2.9% → **記事並みの9%台**（7-1・7-2） |
| 2. `/leaving/netflix` の CTR | クリック上位10 | 1.7% → **4%以上**（7-2） |
| 3. **外部リンククリック率** | GA4 の欄 | 3.3% → **6%以上**（7-1・7-4） |
| 4. 表示回数 | 検索の欄 | 139/日 → 300/日（7-3） |

★ **1と3は独立に動く。** 順位が上がってもクリックの行き先が同じなら3は動かない。
**両方を見ないと、どちらの施策が効いたのか分からない。**

★ **GA4 の数字は最後に見る**（GROWTH 5節の順番は維持）。
ただし**外部リンククリックだけは例外**で、これは Search Console に出ない。
