# 引き継ぎ

最終更新: 2026-09-16

このファイルは**次に作業を再開する人（未来の自分を含む）が最初に読む1枚**。
設計の詳細は他のファイルにあるので、ここでは「今どこにいるか」「次に何をするか」
「同じ穴に落ちないための知識」に絞る。

---

## 1. いま動いているもの

| | 状態 |
|---|---|
| 本番サイト | **https://mihoudairader.com/** （2026-08-23 に独自ドメイン割当済み。`brog-ez1.pages.dev` も生きている） |
| サイト名 | **見放題レーダー**（2026-08-23 に「観とこう」から改名）。理由と却下案は [DEPLOY.md の改名の記録](../DEPLOY.md#取得したら) |
| リポジトリ | https://github.com/MORILABABLE/brog （**Public**。2026-08-25 に API で確認。以前ここには Private と書いてあったが誤り） |
| 収集パイプライン | ✅ 2026-08 で1,089件を取得済み |
| U-NEXT の収集 | ✅ **2026-08-25 に追加**。APIの外側を実ブラウザで取る。配信終了予定210件を収集済み・作品台帳274件。記事は `leaving` の**サービス別バリアント**として実装済み（記事タイプは増やしていない）・1本公開中 → [UNEXT.md](./UNEXT.md) |
| 邦題解決 | ✅ Wikidata経由・解決率73% |
| 人名の日本語化 | ✅ **2026-08-27 追加**。監督・出演者を Wikidata の日本語ラベルで引く（`npm run enrich`）。監督 706/1042件・出演者 594/1042件。引けなかった作品はローマ字のまま出す（記事側が書き分ける）。★ 出演者の並びは**主演順ではない** |
| サイト | ✅ **960ページ生成**・型エラー0（2026-09-13 実測）。うち作品ページ658枚・人物ページ229枚・記事29本。JSは**常設ページの作品検索**（約1KB・インライン）と、**LinkSwitch の外部JS1本**（2026-09-02 から稼働中） |
| 記事 | ✅ **29本**を公開中（2026-09-13 実測。`site/src/content/posts/` は30本で、うち下書き1本） |
| **作品ページ** | ✅ **2026-08-27 追加**。`/works/<作品ID>` を **658枚**（2026-09-13 実測）。1作品につき1枚で「いつまで見られるか」に答える。**LLM不使用**・配信API由来のみ（U-NEXT は載せない）。設計と実行結果は [WORK-PAGES.md](./WORK-PAGES.md)、位置づけは [GROWTH.md](./GROWTH.md)。★ **記事とは双方向につながった** — 記事の表の作品名 → 作品ページ（`rehype-work-links.ts`）、作品ページ → シリーズ記事（`lib/series-for-work.ts`・658枚中86枚・2026-09-13） |
| 記事タイプ | ✅ 5種（`leaving` / `ended` / `arrivals` / `arrivals-service` / `special`）（`npm run write -- --list` が唯一の一覧）。`leaving` は 2026-08-23 に**サービス別**へ分割し、2026-08-25 に U-NEXT を追加。2026-08-27 に `ended` もサービス別へ |
| 特報（`special`） | ✅ **2026-08-27 追加**。「作りたい時期・内容」をそのつど指定して出す記事。軸は**主題**でサービス横断可。`/article` に指示を渡すとフラグに翻訳される（`--kind` / `--topic` / `--slug` が必須）。構成・文体・品質ゲートは月次記事と同じ |
| 記事の軸とタイトル | ✅ **2026-08-27 追加**。記事は**軸（サービス1社／ジャンル1つ／主題1つ）を必ず1本だけ名乗る**。サービス軸に他社を混ぜない・横断はジャンル軸と主題軸だけ・**配信終了はサービス軸のみ**・タイトルは初回も更新版も `【2026年9月】` で始め更新日は本数の直後・同じ月の2本目は作らず書き直す。決まりは `templates/naming.md`、根拠と実測は [ARTICLE-RULES.md](./ARTICLE-RULES.md)。品質ゲートが機械的に検査する |
| 常設ページ | ✅ **9枚**（一覧5＋カレンダー3＋定点観測1）。**LLM不使用**・`collect` のたびに自動更新。実装は `site/src/lib/events-data.ts` |
| ┗ **配信カレンダー** | ✅ **2026-09-07 追加**。上の5枚の**先頭に月の升目**を置いた（赤「終」＝終了・青「新」＝新着。押すとその日の表へ飛ぶ）。**JSは足していない**（素のページ内リンク）。**描くのは今月から先だけ** — 読者の用は「今月のいつ始まって・終わるのか」で、過去2か月の升目は本文を押し下げるだけだった（過去の作品は表には全部並ぶ）。★ 左の枠は**従来どおりカード5枚**（一度3枚に畳んで戻した。理由は [GROWTH.md 3-9](./GROWTH.md)）。★ h1 直上のサービス画像（`LeadImage`）は**外した**。カードのサムネイルとしては残置。★ **表の並びを「きょうに近い順」にそろえた** — 終了予定は近い順（9/7→9/30）、新着は新しい順（9/4→…→7/26）。向きは逆に見えるが規則は1つ。**片方だけ変えないこと**（定義は `lib/events-data.ts` の `loadLeaving` の上） |
| ┗ 補助ページ | `/calendar/netflix` `/calendar/prime-video` `/calendar/disney-plus` の3枚（終了＋新着を1枚に）。**左の枠からは外してあり**、入口は各常設ページ末尾の関連リンクだけ。**あの2本を消すと孤立ページになる** |
| ┗ 配信終了予定 | `/leaving/netflix`（102本） `/leaving/prime-video`（31本）※2026-08-26 時点。`collect` のたびに動く |
| ┗ 新着配信 | `/arrivals/netflix`（212本） `/arrivals/prime-video`（175本） `/arrivals/disney-plus`（17本）※2026-08-26 時点 |
| ┗ 定点観測 | `/stats`（月次の追加・削除。**このサイトにしか出せない数字**） |
| 月別まとめ | ✅ **2026-09-05 追加**。`/archive`（月の一覧）・`/archive/<月>`・`/archive/<月>/<サービス>`。記事はほぼすべてが月次なのに**月で辿る道が1本も無かった**ため（カテゴリ×サービスの一覧は月をまたいで新しい順に積むだけで、「9月のNetflixの記事はどれか」に答えられない）。月は記事の `2026年9月` タグから取る（**`pubDate` は使わない** — 9月の記事は8月末に出る）。束ね方は `site/src/lib/archive.ts`、文言は `config.ts` の `ARCHIVE`。★ **記事がある月・サービスのぶんしか作らない**ので空ページが1枚も無い。★ ヘッダーのメニューは**「監督・出演者」と入れ替えた** — 人物ページの入口は左の枠とフッター（狭い画面ではフッターが唯一。`Footer.astro` の `/person` の行を消さないこと） |
| デザイン | ✅ 濃紺の背景＋本文カード、カテゴリ色分け（[APPEARANCE.md](./APPEARANCE.md)）。**ヘッダーバナーは 2026-08-24 に非表示**（画像はOG生成用に残置） |
| 画像 | ✅ 記事カード（OG）・本文セクション画像・表の作品サムネイルを**ビルド時に自動生成**。**作品ポスター**を使う（提供元の許諾済み・[11節](./APPEARANCE.md#11-作品ポスターの取り扱い許諾取り直し契約終了)）。`heroImage` は生成しているが**記事冒頭では非表示**（一覧カードのサムネイル専用・2026-08-25）。常設ページのサービス画像だけ未配置 |
| 記事の生成（セッション経由） | ✅ `/article` で実行。**API課金なし**。品質ゲートまで検証済み |
| 他社の生き残り検査 | ✅ **2026-09-02 追加**。シリーズ記事を「見放題配信が終了した」に切り替える判定は**その記事の素材だけ**を見ているので、「Netflixで終了・Prime Videoでは配信開始を観測したまま」の作品を見落とす。`pipeline/core/cross-service.ts` が品質ゲート（`--apply`）・`--refresh`・`--list`・**毎日の通知（Issue→メール）**の4か所で拾う。★ **warn。公開は止めない。**「他社で配信中」とは言えない（当サイトが持つのは変化の観測で在庫ではない。Disney+ / Apple TV+ は終了予定を返さず終了の観測も遅れる）ので、渡すのは確かめる材料まで。★ 同じ作品かの判定は**題の正規化**（`ArticleType.sameWork`。IDだけでは U-NEXT の別IDを拾えない）。復帰判定（`revivals()`）は記事の状態を動かすので `work.id` のまま — **判定の重さで使い分けている** |
| 記事の書き直し | ✅ **2026-09-02 追加**。`npm run write -- --refresh` ／ `/refresh`。**終了日が過ぎるのは収集の差分に出てこない出来事**なので、公開済みの記事と今の素材を突き合わせて「見放題終了予定 → 見放題終了」に移るべき記事を割り出す（`core/stale.ts`）。判定に使う指示（`--topic` / `--match`）は**記事のどこにも残らない**ので、書き出すたびに控え `data/articles.json` に記録する（`core/article-log.ts`）。2026-09-02 以前の記事は `--register` で一度だけ登録する。**毎日の通知（Issue）にも出る**（顔ぶれが変わった日だけ）。★ **書き直しは差し替えであって書き下ろしではない** — 前の版の本文がプロンプトに付き、変えるのは事実が変わった箇所だけ（`templates/series.md`「書き直し（状態が変わったとき）」） |
| 月次記事の名乗り直し | ✅ **2026-09-03 追加**。`npm run retire` ／ 毎日 01:19 JST に GitHub Actions（`retire.yml`）が自動で `--write`。**人の確認を挟まない。** 月次記事は書き直さない — タイトルも見出しも本文も月や日付を名乗っていて古くならず、古くなるのは**バッジ（`category`）とタグだけ**。frontmatter の2行を書き換える（`core/retire.ts`）。★ **作品を1本も見ない**のが確認を要らなくしている根拠で、`leaving` の `select()` が「対象月に終了」かつ「終了日が未来」で絞っている以上、月が終われば全件の終了日が過ぎたと確定する。★ 何度実行しても同じ・対象は過去月すべてなので**実行が飛んでも翌日が拾う**。★ `updatedDate` は足さない（本文が変わっていないのに `lastmod` を動かさない）。★ **配信開始側には規則を宣言しない** — 「開始予定日が過ぎた＝始まった」は観測していないので言えない |
| ショート動画の台本 | ✅ **2026-08-25 追加**。`/article` が記事と同時に `shorts/<スラッグ>.md` を作る（`leaving` / `arrivals` のみ。`ended` は意図的に除外）。カット画像は `cd site && npm run shorts` で 1080×1920 を生成。**動画制作そのものは未着手** → [shorts/README.md](../shorts/README.md) |
| 記事の生成（API経由） | 🔶 実装済みだが**実通信は未検証**。`.env` の `ANTHROPIC_API_KEY` は設定済みなので、残るのは `npm run write` を1本通すことだけ |
| アフィリエイト | ✅ 実装完了・**稼働中**。Amazonは**枠別トラッキングIDを含む11変数すべてが本番に反映済み**で、既定ID `jetbike-22` のリンクは0本（2026-09-13）。バリューコマース LinkSwitch も動いているが、**U-NEXT / Hulu は広告主一覧に無く成果にならない**。afb は **Hulu の提携が通って稼働中**（2026-09-14・557ページ。下の「すぐ 1-c」）。❌ **U-NEXT は不合格で、afb では再申請不可**（2026-09-19。下の「すぐ 1-b」）。🔶 Disney+ 単体は審査中。**主線は AdSense（審査中）・Hulu・Amazon の3本**。[AFFILIATE.md](./AFFILIATE.md) |
| 左右の枠 | ✅ **左**＝常設ページのカード（1ページ1枚）、**右**＝新着記事＋広告カード（PRだけ追従）。1200px以上でのみ表示。サムネイルは `site/src/assets/services/` に置くだけ（[APPEARANCE.md](./APPEARANCE.md)） |
| 表の作品リンク | ✅ **2026-08-25 追加**。記事・常設ページの表の全作品が**サムネイル付きリンク**（サイト全体で **3,056本**・2026-09-13 実測）。送り先は `site/src/lib/work-links.ts` の1か所で決める（作品ページの直リンク＞検索）。画像の無い作品はジャンル別の汎用画像 → [AFFILIATE.md](./AFFILIATE.md) の「表の作品リンク」・[APPEARANCE.md 13節](./APPEARANCE.md) |
| 作品検索 | ✅ 常設ページの**「配信カレンダー」の見出しと同じ行の右端**（2026-09-09 に冒頭文の右から移動。冒頭文は撤去）。打つと**候補**が出て、**Enter で**該当作品の行へスクロール（**打っただけでは飛ばない／絞り込みもしない**。2026-09-09 に作り直し）。空にすると窓を画面内へ連れ戻す。全角/半角・大小文字・中黒を無視。ならし方は `site/src/lib/normalize.ts` の1箇所（2026-08-24 追加） |
| 自動デプロイ | ✅ main への push で Cloudflare Pages が自動ビルド |
| **流入・収益の実測** | ✅ **2026-09-06 追加**。`npm run analytics` が Search Console と GA4 を1本のファネルに並べる（表示→クリック→セッション→**外部リンククリック**）。読み取りだけ・サイトには何も出さない。認証は `queries.ts` と同じサービスアカウントで、**GA4 側にも「閲覧者」権限が要る**（`.env` の `GA4_PROPERTY_ID`）。★ **GA4 の数字は本番ホストだけで出す**（`productionHosts()`。既定は `GSC_SITE_URL` のホストと `www` 版、`.env` の `GA4_HOSTS` で上書き）。測定IDはHTMLに書いてあるので誰でも計測エンドポイントに送れて、**GA4 側では止められない**（実測で `mitokou.com` から8セッション。DNSはこのサイトを指していない）。外した内訳も毎回出る。分かったことは [FUNNEL.md](./FUNNEL.md) 8節 |
| 定期実行（収集） | ✅ **稼働中**。毎週 火・金 04:07 JST。8/10・8/13・8/17・8/21 の自動コミットを確認済み（`git log --grep='chore(collect)'`） |
| 更新の通知 | ✅ 収集のたびに **Issue が立ち、GitHubからメールが届く**（2026-08-24 追加）。中身は増減の内訳／新たに判明した終了予定／API枠の消費 |
| API枠の消費 | ✅ `data/api-usage.json` に月別で記録（2026-08-24 計測開始。**8月分は途中からなので過少**） |

| **節の見出しを読者の問いに** | ✅ **2026-09-15 追加**（`plugins/rehype-find-links.ts`）。「他のサービスで探す」の h2 を、ビルド時に**「「ハリー・ポッター」シリーズは終了後どこで観られるか」**へ組み直す。★ **取れていたのは「終了」の語だけだった** — 「ハリーポッター 配信終了」7.2位に対し「ハリーポッター 配信」**47位**・「サブスク」**101位**。需要は横ばいで残っている。★ `<title>`・h1 は `naming.md` の決まりで動かせないので**見出しで取る**。★ **探すのは固定文言のまま**（書き換えるのは見出しの文字だけ）。主題は `data/articles.json` の `flags.topic`。根拠は [FUNNEL.md 9-3](./FUNNEL.md) |
| **記事の「次の一手」** | ✅ **2026-09-15 追加**（`plugins/rehype-next-step.ts`）。記事の**表の直前に B「紹介する◯本のうち△本は[サービス]でも見放題配信中です」**（表の○を数え直した1文＋外部リンク1本・枠は `watch`・**名札は置かない**）、**各表の直後に A「次に読む」**（記事への内部リンク・**記事カードと同じ絵つき**・**表ごとに1本／同じ行き先は1回／最大3本**）を差し込む。★ **記事の本文には他の記事へのリンクが1本も無かった** — 10本すべてが右の枠にあり、あれは1200px未満で消える。読者の**97%はスマホ**。実測と位置（375pxで 4.8→2.1画面目）は [FUNNEL.md 9-3](./FUNNEL.md)。★ **A の行き先は月次 ⇄ 保存版**で、**過去月へは送らない**。★ B は**3本以上かつ全体の20%以上**でなければ出さない（「152本のうち2本」は雑音）。入った本数は記事31本中 A 24本・B 2本 |
| **在庫台帳の穴の通知** | ✅ **2026-09-15 追加**（`pipeline/core/availability-gaps.ts`）。`npm run notify` の本文に「在庫台帳の穴」の節。**印（○△×）を出せていない作品**を、束（キーワード1回で複数作）と単発に分け、**打てるコマンドつき**で出す。★ **実測: 未来に終了する681作のうち、配信API由来は124作・印が出せているのは18作だけ。**残り557作は U-NEXT と告知でカタログに無く、**取りようがない**（出していない）。★ **台帳は14日で古くなる**（読む側が落とす）ので、一度埋めれば済む話ではない。★ **この節は通知を出す理由にしない** — 通知の本数は変わらない。根拠は [FUNNEL.md 9-3](./FUNNEL.md) |
| **U-NEXT の在庫印** | ✅ **2026-09-16 追加**（`site/src/lib/unext-stock.ts`）。記事の表の在庫行に **5社目として「U-NEXT ○ 見放題」**が出る。根拠は `npm run unext:catalog` の**カタログ索引**で、**歩き終えたジャンルだけ**（いまは洋画11,138件・海外ドラマ1,680件。邦画は115/363ページで途中）。★ **○ しか出さない** — × も「—（未確認）」も出さない。歩いていないジャンルの作品は**あっても引けない**ので、不在を言う根拠が無い。★ **出すのは記事だけ。作品ページ658枚と常設ページには出さない**（[GROWTH 2-3](./GROWTH.md) の決定を維持・2026-09-16 に運用者へ確認）。★ 引き当ては**題名の完全一致**で、**割れたら黙る**（索引は `year` を1件も持たない）。★ ポイント作品は ○ にしない（ガイドライン【4】）。終了日が過ぎた作品も落とす。実測: 記事17本・チップ447本・作品261件・**表の直前の1文（B）が2本→7本**・`check:unext` 違反0。設計は [OWN-LEDGER 6-5](./OWN-LEDGER.md) |
| **需要の突き合わせ** | ✅ **2026-09-15 追加**。「いま検索されている作品」と「自前の観測」を突き合わせて記事の主題の候補を出す（`npm run demand`）。取得元は Wikipedia 日本語版の日次 top1000 ／ Google トレンド RSS(JP) ／ サジェスト（確認用）。**無料APIのみ・課金なし。** 実測で7日ぶん3,053語 × 在庫3,738作品 → **候補32件**（うち「いつまで」に答えられるもの7件）。★ **候補を出すところまでで、記事は作らない** — 出口（どの記事にするか）は未着手。設計と引き継ぎは [DEMAND.md](./DEMAND.md) |

### 動作確認済みのコマンド

```bash
npm run collect                 # 配信状況の変化を収集（API消費）
npm run collect:unext           # U-NEXT の新着・終了予定を収集（APIキー不要・数分〜数十分）
npm run unext:refresh           # U-NEXT の作品台帳を取り直す（終了日の変更を見つける）
npm run unext:menu              # U-NEXT のジャンル・カテゴリIDを調べる
npm run notify -- --dry-run     # 通知本文を確認（送らない・API消費なし）
npm run notify                  # 前回以降の変化を Issue で通知
/article                        # このセッションで記事を執筆（API課金なし）
npm run write -- --emit         #   ↑を手動でやる場合: プロンプト書き出し
npm run write -- --apply        #   ↑ response.md を検証して記事にする（台本もここで書き出す）
npm run write -- --dry-run      # プロンプトだけ表示（無料）
cd site && npm run shorts       # ショート動画のカット画像（1080×1920）
cd site && npm run shorts -- --guides   #   ↑ YouTubeのUIが重なる余白を重ねて確認
npm run demand                  # いま話題で、答えを持っている作品を出す（無料API・課金なし）
npm run demand -- --no-fetch     #   ↑ 通信せず、貯めたぶんだけで出す
npm run demand -- --trend "ハリー・ポッターシリーズ"   # 1つの記事の日次推移（第二波の観測）
npm run preview                 # 収集データが記事としてどう見えるか（API消費なし）
npm run catalogs                # 対象国のサービス一覧
npm run typecheck
cd site && npm run build        # サイトのビルド
cd site && npm run build:watch  # 保存するたびに自動でビルド（preview と組で使う・2026-09-14）
```

### 記事生成の2経路

生成手段は2つあるが、**検証・frontmatter組み立て・書き出しは共通コード**（`finalize()`）を通る。
生成手段だけが差し替わる設計なので、どちらを使っても品質ゲートは必ず効く。

| 経路 | コマンド | 費用 | 使いどころ |
|---|---|---|---|
| セッション執筆 | `/article` | **0円** | 手動運用・テンプレ調整中 |
| LLM API | `npm run write` | 約$0.11/本 | GitHub Actions での定期実行（P3） |

---

## 2. ドキュメントの地図

| 読むべきとき | ファイル |
|---|---|
| **「画面のここを変えたい」→ どのファイルか**（早見表） | **[WHERE-TO-EDIT.md](./WHERE-TO-EDIT.md)** |
| **他ジャンルのブログを増やしたい** | **[NEW-THEME.md](./NEW-THEME.md)** |
| **背景・バナー・ロゴ・OG画像・カテゴリ色・記事内の画像** | **[APPEARANCE.md](./APPEARANCE.md)** |
| **アフィリエイト（有効化手順・提携状況・落とし穴）** | **[AFFILIATE.md](./AFFILIATE.md)** |
| **ショート動画の台本を直す・使える素材と使えない素材** | **[../shorts/README.md](../shorts/README.md)** |
| ブラウザ（github.dev）で記事やデザインを直す | [EDITING.md](./EDITING.md) |
| 全体の設計・なぜその選択をしたか | [../DESIGN.md](../DESIGN.md) |
| セットアップ・コマンド一覧 | [../README.md](../README.md) |
| ドメイン取得・GitHub・Cloudflare の操作手順 | [../DEPLOY.md](../DEPLOY.md) |
| **記事の軸・タイトルの決まりと、その根拠** | **[ARTICLE-RULES.md](./ARTICLE-RULES.md)** |
| **検索流入が0のまま — 何を増やすか・どのデータを出してよいか** | **[GROWTH.md](./GROWTH.md)** |
| **★ 実測でどこが詰まっているか（GA4・Search Console）・目標までの算数** | **[FUNNEL.md](./FUNNEL.md)** |
| **記事がフローで消えていく — 何が積み上がるか・記事タイプをどう増やすか** | **[STOCK.md](./STOCK.md)** |
| **作品ページ（`/works/<id>`）を実装する** | **[WORK-PAGES.md](./WORK-PAGES.md)** |
| **いま検索されている作品から主題を選ぶ（需要 × 在庫の突き合わせ）** | **[DEMAND.md](./DEMAND.md)** |
| 記事の構成・文体を変えたい | `theme-packs/streaming-jp/templates/leaving.md` ほか |
| 記事の軸とタイトルの決まりそのもの（プロンプトに入る） | `theme-packs/streaming-jp/templates/naming.md` |

---

## 3. 次にやること（優先順）

### ストック側の施策は STOCK.md にまとめてある

**[STOCK.md](./STOCK.md) の S-1〜S-7 が、積み上がる形にするための施策の全部。**
この節（すぐ／そのあと）は運営と収益の段取りで、あちらは中身の作り方。**別の軸なので混ぜない。**

| | 施策 | 状態 |
|---|---|---|
| S-1 | 記事の表を作品ページへ＋関連リンク | **✅ 2026-08-29** |
| S-2 | 作品ページの「配信の記録」 | **✅ 2026-08-29** |
| S-3 | 人物ページ（**229枚**・2026-09-13 実測） | **✅ 2026-08-30** |
| S-4 | 記事タイプ『シリーズ』（月を名乗らない記事） | **✅ 2026-08-30**。記事10本・作品ページ658枚中86枚をカバー |
| S-5 | 記事タイプ『観測レポート』 | 未着手（観測3か月ぶん待ち） |
| S-6 | 収益（Phase B の提携申請） | **ほぼ決着**（2026-09-19）。Amazon 完了（枠別IDまで分離・2026-09-13）／afb は **Hulu のみ合格・U-NEXT は不合格（再申請不可）・Disney+ 単体は審査中**。**主線は AdSense（審査中）・Hulu・Amazon**＝下の「すぐ 1-b」 |
| S-7 | 運用の配分（月1本をストック側へ） | 未着手。**回遊の実測（[FUNNEL.md 9-2](./FUNNEL.md)）がこれを裏づけた** — シリーズ記事1本でカバー枚数が増える |

> **✅ 2026-09-06 に測った。結果は [FUNNEL.md](./FUNNEL.md)。**
>
> | | 実測（2026-08-29〜09-03） |
> |---|---|
> | 作品ページ | 517枚中 **135枚に表示が付いた**。表示412件（サイト全体の50%）・クリック13・**CTR 3.2%** |
> | 人物ページ | 索引対象31枚のうち**2枚に表示・計2件・クリック0** |
> | 記事 | 13本で表示233件・クリック23・**CTR 9.9%** |
>
> **判断: 人物ページの下限を下げない。作品ページ第2弾も出さない。**
> 理由は枚数ではなく**中身**で、いまの作品ページは89%が「終了しました」で終わっていて、
> 同じ順位帯の記事の 1/3.4 しかクリックされていない（[FUNNEL.md 3節](./FUNNEL.md)）。
> **その型のまま枚数を増やしても比率が悪化するだけ。**
> 先に [FUNNEL.md 7-1・7-2](./FUNNEL.md)（作品ページの答えとタイトルを変える）をやる。

### ✅ 決着（2026-08-25）: ポスターの再ホストは許諾を得た

**Movie of the Night（配信API提供元）から「再ホストしてよい」との回答を得たので、
セクション画像に作品ポスターを載せる改修を実装した。**

| 照会した内容 | 回答 |
|---|---|
| ビルド時に取得・変換して**自分のドメインから配信してよいか** | **可**（"Yes, you can."） |
| 契約終了時には保存した画像を消す義務があるという理解でよいか | 「その条項は更新された。最新版を見てほしい」 |
| 失効する画像URLの推奨リフレッシュ間隔 | **最低でも6ヶ月ごと**に取り直すこと。★ **これは回答文の推奨であって、規約上の義務ではない**（2026-09-20 に現行規約6節の原文で確認。6節は「必要なあいだ手元に保存してよい」と書くだけで期限を置いていない）。取り直しは月ぶんずつの平準化に変えた |

> ✅ **更新後の規約は 2026-09-17 に読んだ**（[PARTNERSHIPS.md 3-1](./PARTNERSHIPS.md)）。
> - 6節: 画像を**手元に保存して使い続けてよい**。**契約終了後も**保持・使用してよい
> - 5節: ポスターは第三者の権利物で、**提供元は著作権の許諾を出さない（出せない）**。
>   上の「可」は**自ドメインから配信してよいか**への回答であって、権利者の許諾ではない。
>   提供元から通知があれば、該当画像を速やかに消す義務がある
> - 4節: 画像も出すなら帰属表示に**画像も**書く → `ATTRIBUTION` に「作品画像」を足した
>
> 以下の「契約終了時には消す」前提の記述は、当時の厳しい側の読み。
> **実装は「契約終了時には消す」という厳しい側の前提で組んである**
> （画像を git に入れない・`npm run posters:purge` で全部消せる）ので、
> 規約が緩くなっていた場合でも作り直しは要らない。

**入れたもの**

| | |
|---|---|
| ポスターの取得・キャッシュ | `site/scripts/posters.mjs` |
| セクション画像への合成 | `site/scripts/make-sections.mjs`（ポスターあり／文字だけ の2版） |
| 表の作品サムネイル | `site/scripts/make-thumbs.mjs` → `site/public/thumbs/` |
| 画像が無い作品の汎用画像 | `site/scripts/genre-art.mjs`（ジャンル別26枚） |
| 使っている作品の台帳 | `data/image-manifest.json`（`npm run sections` と `npm run thumbs` が書き出す。**2026-08-25 に58件→590件**。取り直しは2回に分ける） |
| 取り直し（**月ぶんずつ平準化**・2026-09-20〜） | `npm run refresh:images`。台帳1,800件に対し月80件を上限に、期限の近い順。**義務ではない**（失効しても手元の原本で掲載は続く） |
| 契約終了時の全削除 | `cd site && npm run posters:purge` |

**運用で覚えることは1つだけ。**
月に一度 `npm run refresh:images` を叩く。今月ぶんが済んでいれば何もせず終わる。

★ **2026-09-20 に急かすのをやめた。** それまでは `npm run sections` が
「期限が近い → 取り直してください」と警告していたが、規約の変更で
**失効しても手元の原本で掲載が続く**ようになり、かつ取り直しは月80件に平準化された。
毎ビルド出るのに月の大半は何もできない警告になるので、ただの報告に変えてある。
放置しても記事は壊れない（手元に原本が無い環境でだけ、その節が文字だけのカードに戻る）。

詳細は [APPEARANCE.md の11節](./APPEARANCE.md#11-作品ポスターの取り扱い許諾取り直し契約終了)。

**見せ方（2026-08-25 に一度作り直した）**

- 画像は**小見出しの直後**に入る（見出し → 画像 → 表 → 地の文）
- ポスターが揃った節は**絵だけ**。枠も日付も見出しも描かない
  （すぐ上の見出しと同じ文字が2回出て冗長になるため）
- 揃わなかった節だけ、日付＋作品名の**枠つきカード**を出す
- ポスターは **Amazonのビデオ内検索へのリンク**を兼ねる。
  記事末尾まで読まれないと出なかった Amazon 導線が、**節の数だけ**現れる
  （[AFFILIATE.md](./AFFILIATE.md)）

**実測（2026-08-25）**

- 37節中 **30節がポスター**、7節が文字だけの版
- 取得は **1ビルドあたり58枚・3.3MB**（帯域の無料枠は 1GB/月）← 2026-08-25 時点。**表のサムネイルと作品ページを入れて千枚前後・50〜90MB／ビルドに膨らんでいた**ため、2026-09-17 からビルドをまたいで持ち越す形にした（[APPEARANCE.md 11節](./APPEARANCE.md)）
- 取り直しの対象は **58作品** ＝ APIリクエスト58回（無料枠は 500回/月）
- 画像URLの期限は最短 **2026-12-21**
- 表示はブラウザで確認済み（1280px / 390px の両方で2枚が横に並ぶ）

> **作中キャプチャ（本編の場面写真）は使えない。** 調査済み。
> APIが返すのはポスターとキーアートのみ、Wikidata/Commons にも無い（実測0件）、
> 自前のスクリーンショットは商用サイトでは引用の要件を満たさない。
> 経緯は [APPEARANCE.md の9節](./APPEARANCE.md#9-記事ごとのカード画像)。

**記事のヘッダー画像（frontmatter の `heroImage`）も自動で入るようにした。**
選び方は「記事タイトルと一致する作品」→「記事に最初に出てくる作品」。
**手で別のパスを書けばその記事は自動処理から外れる**ので、絵は好きに変えられる。
実測で7本中6本に入った（残り1本はU-NEXTのみの記事で素材が無い）。
→ [APPEARANCE.md の12節](./APPEARANCE.md#12-記事のヘッダー画像frontmatter-の-heroimage)

> ★ **いま出るのは記事一覧のカードの左サムネイルだけ。**
> 記事ページの見出し上は 2026-08-25 に非表示にした。表の全作品に
> サムネイル付きリンクが並ぶようになり、冒頭にもう1枚置く意味が無くなったため
> （本題の表に届くまでのスクロールを削るほうが効く）。
> 生成も frontmatter もそのままなので、戻すのは1行。

> **常設ページのサービス画像（`N` `A` `D` の頭文字タイル）は別系統で、まだ空。**
> `site/src/assets/services/{netflix,prime-video,disney-plus}.png` を置けば出る。
> 作品のポスターはサービスを表さないので、ここは自動化していない。

**常設ページの見出しに基準日を出すようにした**（2026-08-25）。

| 出る場所 | 形 |
|---|---|
| ページの `<title>` / `<h1>`、一覧カードの見出し | `【2026年8月25日時点】Netflixで配信終了予定の作品一覧` |
| 常設枠（左の枠）のカード | `【2026年8月25日更新】Netflix` |

常設ページは公開日を持たず中身だけが入れ替わるので、いつの情報かを先に伝える。
**ページを増やすときも同じ形にすること。** 組み立ては
`site/src/lib/evergreen.ts` の `evergreenTitle()` / `evergreenStamp()` だけが行う
（[DESIGN.md 5-1](../DESIGN.md#5-1-常設ページllm不使用)）。

### ★ 次セッションの積み残し（2026-09-16）

> このセッションは**「U-NEXTの在庫が取れるようになったので、そこで流入と収益を伸ばしたい」**
> から始まった。調べたところ、**索引は9日前から揃っていて、読む人が誰もいなかった。**
> 設計と実測は [OWN-LEDGER 6-5](./OWN-LEDGER.md)。

#### この日にやったこと

| | 作ったもの／直したもの | どこ |
|---|---|---|
| 1 | カタログ索引を読む1枚（規律もここに全部） | `site/src/lib/unext-stock.ts`（新規） |
| 2 | 記事の表の在庫行に **5社目として U-NEXT の ○** | `site/plugins/rehype-availability.ts` |
| 3 | 4社が1つも引けない行でも、U-NEXT が引ければ答えを出す | 同上（`availRow` の `!marks` 分岐） |
| 4 | 凡例の社数を「実際に出た印」に合わせる（4／5） | 同上（`legendNode`） |
| 5 | ドキュメントを実態に合わせた | OWN-LEDGER 6-5／CROSS-SERVICE 3-2・9-3／GROWTH 2-3 |

**`npm run` のコマンドは1つも増えていない。** 収集は今までどおり週2回の
`collect-unext.yml` が進め、記事側は**ビルドのたびに索引を読み直すだけ**。

#### 🔴 引き継ぐべき副作用 — B の行き先が Amazon から U-NEXT に移った

表の直前の1文（B・`rehype-next-step.ts`）は **「○ が最も多いサービス」**を選ぶ。
U-NEXT の ○ が入ったことで、**7本すべてが U-NEXT を名指すようになった。**

| | 2026-09-15 | 2026-09-16 |
|---|---|---|
| B が入った記事 | 2本（Amazon） | **7本（すべて U-NEXT）** |

★ **選び方は変えていない。** 「紹介料の高い順に並べない」と
  [AFFILIATE.md 7節](./AFFILIATE.md) で決め、プライバシーポリシーにも書いてある。
  U-NEXT が選ばれているのは**本当に ○ が多いから**で、ここを報酬で覆すと方針違反になる。

★ **いまの U-NEXT の B リンクは1円も生まない**（afb 未提携）。
  一方で Amazon だった2本ぶんの成果も消える。**ただし実損はほぼ無い** —
  `PUBLIC_AMAZON_TAG_WATCH` は今も未設定（＝既定IDに落ちていて枠別に読めない）で、
  GA4 の `aff_watch` は 2026-09-15 時点で **0クリック**だった。

❌ **その「通った日」は来なかった**（2026-09-19 afb 不合格・再申請不可。
  [AFFILIATE.md 14節](./AFFILIATE.md)）。
  **それでも B の選び方は変えない。** 送り先は○が多いサービスで選んでおり、
  **報酬で覆さない**と決めてある（上の★と AFFILIATE 7節）。
  落ちたから Amazon に付け替える、は方針を壊す。
  **別ASPで U-NEXT が通れば、下の話がそのまま生きる。**

★ **afb の U-NEXT 提携が通った日に、この7本が一気に価値を持つ。**
  B の CTA（「U-NEXTで観る」）は**サービス単位の導線**なので、
  ガイドライン【5】の「LP以外の成果は却下」と**形が合っている**
  （作品別リンクは成果にならない。[AFFILIATE.md 11-6](./AFFILIATE.md)）。
  ただし LP に差し替えると**それは広告**になるので、掲載NGの門（`unext-ad.ts` の
  `ngHitsIn`）とPR表記を通すこと。**いまの実装は検索リンクのままで、広告ではない。**

#### 次にやること（この順）

1. **afb の返事を確認する**（[AFFILIATE.md 12-0](./AFFILIATE.md)）。2026-09-03 申請から2週間。
   通っていれば 12-3 の手順。**返事が無ければ afb に催促してよい時期**
2. **邦画の周回が終わるのを待つ**（いま115/363ページ）。週2回×250ページで自動的に進む。
   歩き終わった次のビルドから ○ が増える。**手で何かする必要は無い**
3. **測る**（2026-10-01 以降・下の「A. 内部リンクの効果を測る」と同じ日に）
   - `/posts/harry-potter` の「ハリーポッター 配信」（2026-09-15 時点 **47位**）と
     「サブスク」（**101位**）が動くか。**「終了」の語（7.2位）ではなく、この2つで見る**
   - GA4 の外部リンククリックで U-NEXT が出てくるか（`npm run analytics`）
4. **`PUBLIC_AMAZON_TAG_WATCH` を作って .env と Cloudflare Pages の両方に足す**
   （2026-09-15 から持ち越し。**Pages を忘れると本番だけ既定IDに落ちる**）

> ★ **「U-NEXTに無い」と書けるようにはなっていない。** 歩き終えたのは11ジャンル中2つ。
>   邦画・アニメ・国内ドラマの作品は**あっても引けない**ので、
>   ○ が付いていないことを「無い」と読む実装を足さないこと。

### ★ 次セッションの積み残し（2026-09-15）

> このセッションは**「検索が多い語を調べて、自分の在庫と照会し、訴求記事を作りたい」**から始まった。
> 調べた結果、**層1（需要の取得）と層2（在庫との突き合わせ）だけを実装し、出口は保留にした。**
> 設計・実測・未着手はすべて **[DEMAND.md](./DEMAND.md)** にある。この節はその要約。

#### この日にやったこと

| | 作ったもの | どこ |
|---|---|---|
| 1 | 需要の取得（3つ） | `pipeline/sources/demand/`（wikipedia-pageviews / google-trends / google-suggest） |
| 2 | 貯める（取得元で貯め方が違う） | `pipeline/core/demand-store.ts` → `data/demand/YYYY-MM.jsonl` |
| 3 | 在庫との突き合わせ（誤爆の歯止め5つ） | `pipeline/core/demand-match.ts` |
| 4 | コマンド | `pipeline/cli/demand.ts`（`npm run demand`） |
| 5 | 人が捨てた語の置き場 | `data/demand-ng.json`（初期は空） |

#### 次にやること（この順）

1. ~~**ハリー・ポッター記事の分析**~~ → **✅ 同日に実施し、A/B を実装した**（[FUNNEL.md 9-3](./FUNNEL.md)）。
   原因は「**記事の本文に他の記事へのリンクが1本も無い**」ことだった。
   残っているのは効果の確認だけ（下の「測る」）
2. **出口を決める**（[DEMAND.md](./DEMAND.md) 6-2・6-3）。記事生成を主にするか、
   既存ページの押し出しを主にするか。**実測は後者に収益があると言っている**
3. 1〜2週間ぶん `npm run demand` の候補を眺める。**その前に重みを決めない**

#### 測る（2026-09-15 に出した手）

- 🔴 **`PUBLIC_AMAZON_TAG_WATCH` を作って .env と Cloudflare Pages の両方に足す。**
  未設定の枠は既定IDに落ちる（成果は出るが**枠別に読めない**）。
  **Pages 側を忘れると本番だけ既定IDに落ちる**（2026-09-13 に一度踏んだ穴）
- `npm run analytics` の「読まれたのに次へ1本も出ていないページ」から
  `/posts/harry-potter` が消えるか
- GA4 の `aff_watch` が 0 から動くか

> ★ **候補の数字（閲覧数・検索数）は記事に書かない。** 当サイトの観測ではないものを
>   当サイトの主張として出すことになる。主題を選ぶまでの手がかりとして使う。

### ★ 次セッションの積み残し（2026-09-13）

> このセッションは**「Amazonの成果が1件出た。どちらのリンクか」**から始まって、
> 導線の詰まりを実測で潰していった。数字はすべて実測
> （Search Console 2026-08-25〜09-12 / GA4 2026-09-01〜13）。
> 詳しい測定値は [FUNNEL.md](./FUNNEL.md)、Amazon側の事情は [AFFILIATE.md](./AFFILIATE.md)。

#### この日にやったこと（コミット済み: 9b71401 / 86ee798）

| | 直したこと | 実測 |
|---|---|---|
| 1 | **Prime無料体験の枠だけ本番で既定IDに落ちていた**（`PUBLIC_AMAZON_TAG_PRIME` が Pages 未登録）。成果は取れるが**切り分けができない** | 本番HTMLが `amazonprime?tag=jetbike-22` |
| 2 | ○/△ の印が `app.primevideo.com` を向いていた。**`tag=` が乗らない＝0円**。落とし先の規則を `work-links.ts` の `availabilityUrl()` に1か所で置いた | tag=付き **38 → 60**／該当0件に |
| 3 | ○/△ のチップが**リンクに見えなかった**（手がかりが `:hover` だけ＝タッチ端末に一生出ない）。平常時から accent の枠と文字を出し、タップ領域もチップ全体に広げた | セッションの**71%がスマホ**。`aff_avail` は0クリックだった |
| 4 | 「他のサービスで探す」を**ビルド時に組み直す**（新規 `plugins/rehype-find-links.ts`）。LLM が書いていて中身がばらばらだった（ハリポタ11/11・Netflix 12/63・**U-NEXT 0/80**）。ここに **Amazon を足した** | `find` 枠 2,752 → 7,019 |
| 5 | **印を1件も出せない記事の表に「他で探す」行**（`rehype-availability.ts` の `findOnlyRow`）。それまで歯止めが `return` していて、表に次の一手が1つも無かった | U-NEXT記事の Amazon リンク **15 → 177** |
| 6 | トップに**「最新記事」の名札**＋**12本で打ち切り**（溢れは `/archive`） | トップ 87KB → 69KB・h2 29 → 12 |
| 7 | **作品ページ → シリーズ記事の内部リンク**（新規 `lib/series-for-work.ts`）。下の A の本体 | 658枚中 **86枚**に |
| 8 | **作品ページの「他のサービスで探す」にも Amazon を足した**（`works/[id].astro`・**未コミット**）。4番で記事側には足したのに、**作品ページ658枚は U-NEXT / Hulu / DMM TV の3社だけ**＝提携が無く1本も成果にならない節のままだった。枠は `work` ではなく `find`（`data-slot` と揃える） | `find` 枠 1,382 → **2,040**本 |

サイト全体で **tag= 付き Amazon リンク 6,914 → 8,099**（8番まで入れた最終ビルドで **8,768**）。
tag= 無しは5本だけで、`aboutamazon.jp`（出典）4本と `/privacy` の規約リンク1本
＝**付けてはいけないもの**だけ。`npm run check:unext` も「違反も注意もありません」。

#### 次にやること

**A. 内部リンクの効果を測る（2026-10-01 以降）** ← 最優先。**手を動かす前にこれ**

作品ページ同士が**シリーズ級の検索語で食い合っていた**のが、作品ページのCTRが
記事の半分しかない主因だった（順位ではなくタイトルの問題、ではなかった）。

```
「ハリーポッター ネトフリ いつまで」（2026-08-25〜09-12）
    9.0位 /works/536 ・ 10.0位 /works/446 ・ 10.3位 /works/428 ・ 10.3位 /works/548
    → 表示13・**クリック0**。CTR 10.2%・7.5位の /posts/harry-potter はこの語に出ていない
```

7番の内部リンクは**その食い合いを解くための第一手**で、効くまで数週間かかる。
**判定は「作品ページのCTR」ではなく、食い合っていた検索語の順位で見ること。**
狙いは `/posts/harry-potter` が1枚で上がること。
効かなければ **(B) canonical → (C) noindex** と進む。
★ **いきなり B・C に行かないこと。** 単作の検索語（「スリザー 配信」「失楽園 netflix」など、
`/works` の表示の大半）は**作品ページが正しい受け皿**で、そこまで捨てることになる。

**B. 🔴 月次記事に `--ids` で在庫を埋めるのは効かない（2026-09-13 に8件使って確認）**

> **前の版はここに「あと1件・あと3件でゲートを越える」と書いていたが、間違いだった。**
> 実際に `2026-09-arrivals-prime-video` へ8リクエスト使って、**表示は1行も増えなかった。**

**理由は枠ではなく仕組みのほう。**

- 在庫行は**その行のサービスの印を出さない**（`rehype-availability.ts` の `availRow` が
  `own` を除く）。「Netflixで終了」の行に「Netflix ○」を出すと読者が判別できないため
- ところが**新着記事の作品は「そのサービスに今入った」もの**で、他社には無いことが多い
- 実測: 8件中 **6件が Prime Video のみ**、他社ありは2件だけ（Apple TV+ のレンタル＝△）
- ＝ **サービス軸の記事に、そのサービスの作品の在庫を足しても印は増えない**

| | 実測（2026-09-13） |
|---|---|
| `2026-09-arrivals-prime-video` | 作品29件・在庫あり1件 → 8件取得しても**印が付くのは2件**。ゲート（1/3＝10件）に届かない |
| `2026-09-leaving-prime-video` | **すでにゲートを通っていた**（31行中10行に印）。「あと1件」は誤り |
| 取得した8件の作品ページ | **1枚も無い**（この8件は作品ページの対象外）。表示面の改善もゼロ |

★ **在庫取得が効くのは「サービス横断の面」だけ。** シリーズ記事（`series`）と
作品ページ `/works/<id>`（4サービスぶんを並べる）。**月次記事には使わないこと。**

★ 枠の残り: 9月は上限500・**使用286**（この8件を含む）・月末までの予約196
＝ **自由枠18**。10月の枠は FUNNEL 7-1 の第2段（**作品ページ**の在庫）に回す。

**C. ✅ Cloudflare Pages の環境変数（2026-09-13 完了）**

**11変数すべて登録済み**で、本番HTMLに既定ID `jetbike-22` のリンクは**0本**
（`/`・`/works/80`・`/posts/harry-potter`・`/leaving/netflix`・月次記事で確認）。
これで**10月のアソシエイト管理画面は枠ごとに読める**。
`prime022-22`（無料体験・固定報酬500円/件）も他と混ざらなくなった。

> 🔴 **この日の教訓: `.env` に足したら、その場で Pages にも足すこと。**
> `_WORK` と `_PRIME` は `.env` にあるのに Pages だけ抜けていた。
> ローカルビルドでは正しく分かれて見えるので、**本番HTMLを見ない限り気づけない。**

★ **環境変数だけを変えたときはビルドが走らない。** Deployments → 最新 → Retry deployment。
★ **アソシエイト・セントラルに実在するIDだけを書くこと。** 未登録IDの成果は計上されない。
★ 入ったかどうかは本番HTMLで見る（DEPLOY.md「アフィリエイトの環境変数」の `curl` 一式）。

**D. ~~U-NEXT記事の ○/△ は、いまの道具では出せない~~ → ✅ 2026-09-16 に別の道で出した**

> **結論そのものは正しかった** — 配信APIの側からは今も繋がらない。
> 繋がったのは**逆方向**で、`npm run unext:catalog` が作っていた
> **U-NEXTのカタログ索引**を記事の表に読ませた（[OWN-LEDGER 6-5](./OWN-LEDGER.md)）。
> 下に「別の道（カタログ同士の照合）を作るまで保留」と書いてあるのが、その道。
>
> ★ **当時「完全一致は0件」と測ったのは、台帳のWikidata由来の邦題32件**が相手だった。
>   実際に効いたのは**変化ログの `localizedTitle`**（3,386作品が持っている）で、
>   索引16,238件と突き合わせると **1,177件が一致**した。
>   **測る相手を間違えていた**だけで、道は最初から通っていた。

以下は 2026-09-13 時点の記述（配信API側から見た事情は今も変わらない）。

9月のU-NEXT記事80作品のうち、**74件が配信APIのカタログに無い**（U-NEXTの `SID…` だけ）。
枠を積んでも取れない — `api_language: en` でキーワードは英題に当たるが、
U-NEXT由来の作品は原題を持たない。仮に取れても繋がらない：
台帳のWikidata由来の邦題32件と U-NEXTカタログの邦題1,903件で**完全一致は0件**
（`ledgerIdsForTitle` は完全一致）。
**別の道（カタログ同士の照合）を作るまで保留。** 5番の「他で探す」行が当面の答え。

**E. ✅ 回遊は測った（2026-09-13）。結果は [FUNNEL.md 9-2](./FUNNEL.md)**

`npm run analytics` に「■ 回遊（どこからどこへ移っているか）」を足した
（GA4 の `pageReferrer` が自サイトなら1つ前のページ。**管理画面にはこの口が無い**）。

| 30日（2026-08-14〜09-10） | |
|---|---|
| 回遊PV | **61 / 355 = 17.2%** |
| 作っているのは | `/category/*` とトップ（合わせて回遊の **37.7%**） |
| 🔴 `/works/*` から次へ | **0件**。658枚が行き止まり |
| 🔴 `/posts/harry-potter` | 36PVを集めて内部リンクが**1本も踏まれていない** |

**✅ 2026-09-14 に手を2つ出した**（[FUNNEL.md 9-2](./FUNNEL.md)「出した手」）。

| | やったこと | 実測 |
|---|---|---|
| 1 | 記事へ戻る道を**月次記事にも**広げた | 作品ページの記事リンク **91枚 → 253枚** |
| 2 | 関連作品の先頭を**「同じ日に終わる作品」から外した** | 先頭が「同じ日」のページ **大半 → 0枚** |
| 3 | 「次の1本」を**配信状況の直後**へ（スマホの71%に効く） | 253枚で**本文の最初の内部リンク**に |
| 4 | 出演者を**3人でたたんだ**（`<details>`） | 見える `/person` リンク 820 → 425本（HTML内は820本のまま） |

★ **母数が小さい**（回遊PV 61件）。0件は「弱い」の証拠で「効かない」の証拠ではない。
★ **この期間は 2026-09-13 の内部リンク（作品ページ→シリーズ記事86枚）が入る前。**
  次は **A と同じ 2026-10** に読む。増やす手は**シリーズ記事を1本書くこと**
  （10本で86枚をカバー。記事を足すとカバー枚数が増える＝S-7）。

**F. スマホの表の横スクロール**

月次記事の表は横スクロールが要る。列を落とさない判断は 2026-09-06 に
意図的にされたもの（`rehype-availability.ts` の `applyPlan`。`dropColumn` は残してある）
なので、**変えるなら運用者と相談してから。**

---

### すぐ

1. ✅ **アフィリエイトは有効化済み**（Amazon・LinkSwitch は 2026-09-02、枠別IDは 2026-09-13）
   `PUBLIC_AMAZON_TAG` / `PUBLIC_VC_LINKSWITCH_PID` は `site/.env` と Pages の両方に入り、
   本番で動いている。**枠別トラッキングIDも11変数すべて反映済み**（上の C）。
   🔴 **`.env` に足したら、その場で Pages にも足すこと。**
   `.env` はリポジトリに入らないので、**Pages を忘れると本番だけ古いまま**になる。
   ローカルビルドでは正しく分かれて見えるので、**気づけるのは本番HTMLだけ**
   （`_WORK` と `_PRIME` が実際にそうなっていた。上の C）。
   手順と落とし穴は [AFFILIATE.md](./AFFILIATE.md) と [../DEPLOY.md](../DEPLOY.md)。
1-b. ❌ **U-NEXT（afb）は不合格。afb では二度と申請できない**（2026-09-19）
   **サイト側の作業は無い。** U-NEXT の広告は `.env` 未設定で元から出ていないので、
   **撤去も再ビルドも不要**（落ちたことで壊れたものは1つも無い）。
   要因の推測と**次のASPの進め方**は **[AFFILIATE.md 14節](./AFFILIATE.md)**。

   | 待っていたもの | 結果 | いま何をするか |
   |---|---|---|
   | afb / U-NEXT の提携審査 | ❌ **2026-09-19 不合格**（**afb は落ちた媒体を再審査しない**） | **12-3 は実行しない。** やるなら **A8.net などの別ASPで受け直す**（14-3） |
   | afb / Hulu＋Disney+ パック | ❌ **2026-09-19 不合格** | ディズニーが絡む案件だけ落ちている（14-2 の F） |
   | afb / Disney+（単体） | 🔶 **審査中**（申請は生きている） | **待つだけ。作業は無い。** 期待値は低い |
   | 掲載NGの解釈の回答 | ✅ **2026-09-16 回答あり（B で確定）** | **実装の変更なし。** 判定は Hulu 側で現役（`npm run unext:ng` を止めない） |

   ⛔ **12節の U-NEXT 実装を消さないこと。** `.env` が空なら1バイトも出ず、
   **別ASPで通った日に値を差し替えれば戻る**（afb 専用なのはリンクコードと `id1` だけ。14-3）。
   **掲載NG一覧と `check:ads` は Hulu が使っている**（14-5）。

   ⛔ **別ASPに出すのは、数字が変わってから**（14-4）。広告主は同じ U-NEXT なので、
   **いまのまま出せば同じ理由でまた落ちる。**

1-c. 🟢 **Hulu（afb）は提携が通り、稼働している**（2026-09-14）
   実装・検査まで済んでいる。詳細は [AFFILIATE.md 13節](./AFFILIATE.md)。
   **1ページに afb の枠は1つで、U-NEXT が出せない面に Hulu が出る**（実測557ページ＝作品541・記事16）。

   | いますぐやること | なぜ |
   |---|---|
   | **Hulu の環境変数4つを Cloudflare Pages に入れる**（`PUBLIC_AFB_HULU_LP` / `_IMP` / `_BANNER` / `_BANNER_SIZE`） | `site/.env` には入れた。**Pages を忘れると本番だけ広告が出ない。** 3つは同じ原稿から取ったものを揃えること（DEPLOY.md） |
   | ~~`npm run unext:ng` を流す~~ | ✅ **済**（2026-09-14）。TBS/日テレ/FOD を分けた。Hulu が見るのは TBS 1,478件だけになり、広告が 533 → 557ページに増えた（13-6） |
   | afb 経由で Hulu に解釈を確認する | U-NEXT と同じ論点。聞く内容は 13-7 に下書きしてある |

   ⛔ **記事の Hulu 検索リンク（824本）を afb リンクに置き換えないこと。**
   当サイトは Hulu の配信状況を取得できない（規約で禁止）ので、
   **「配信中の作品のみ使用可」を作品単位で満たせない。**
   置き換えると824本すべてが未配信タイトルの訴求になりうる＝提携解除・成果全却下。
   理由の全文は 13-1。

   **2つは独立している。** 提携が先に通ったら、解釈の回答を待たずに公開してよい
   （いまの実装は安全側に倒してある）。
   置き場所・文言・掲載NG作品の除外はすべて実装済みで、
   **`.env` に入れるまでは広告も vref.js も1バイトも出ない。**
2. ✅ **連絡先は設定済み**（`site/src/pages/contact.astro` の `CONTACT.email`）
   本番の `/contact` に警告は出ていない。`CONTACT.formUrl`（Googleフォーム）は空だが、
   **メールか フォームのどちらかがあれば警告は消え、AdSense の要件も満たす。**
3. **`.env` に `ANTHROPIC_API_KEY` を入れて `npm run write` を実通信で検証**
   プロンプト組み立てまでは確認済みだが、生成〜検証〜書き出しの通しは未検証。

> ✅ **完了: `mihoudairader.com` の Cloudflare Pages への割り当て**（2026-08-23）
> 詰まったときの原因（左メニューに `Pages` が無い等）は
> [DEPLOY.md の4節](../DEPLOY.md#4-独自ドメインを割り当てる)。

### そのあと

4. ✅ 記事は **29本**（2026-09-13）。ASP審査の本数条件（10〜15本）は満たしている
5. 記事タイプを1つ増やす
   → ★ **候補は `ranking` ではなく『テーマ』**（[STOCK.md](./STOCK.md) の S-4）。
     いまの記事タイプは**全部スラッグに月が入る**ので、URLが毎月変わって評価が積み上がらない。
     月を名乗らない記事タイプを1つ作るほうが、ランキングを足すより効く
   → 最も新しい実装例は `theme-packs/streaming-jp/article-types/special.ts`
   → 新しい記事タイプは **`axis`（サービス／ジャンル／主題）の宣言が必須**（型が通らない）
   → `special` は**フラグ宣言（`ArticleType.flags`）とカテゴリの実行時決定（`categoryOf`）**
     を使っている。CLI を触らずに記事タイプを増やせる仕組みはここを読む
6. `upcoming` を収集の既定から外すか判断する
   → 全実行・全サービスで**0件**が続いており、月12リクエストの空振り。
     API側が返し始める可能性はあるので、四半期に一度 `--kinds upcoming` で手動確認する運用も可
   → ★ **配信開始予定そのものは各社の告知から取れるようになった**（2026-08-28）。
     `npm run collect:announce` / [ANNOUNCEMENTS.md](./ANNOUNCEMENTS.md)。
     解析器があるのは Prime Video だけなので、**次は Disney+ か U-NEXT を足す**
     （どちらも前月25日〜月末に告知が出る。URLの規則性は ANNOUNCEMENTS.md 3節）
7. `bench.ts`（同じ素材を各LLMに流してコスト・品質・検証通過率を比較）
8. GitHub Actions で記事のPR自動作成（P3）
   → **収集の定期実行と更新通知は稼働済み。** 残るのは記事生成を Actions から回す部分
9. AdSense 申請（P4）。**本数条件（20〜30本）は 29本で満たした**（2026-09-13）。
   `PUBLIC_ADSENSE_CLIENT` は未設定なので広告は1本も出ていない。準備は AFFILIATE.md 10節
10. ✅ アフィリエイト Phase C（**月次記事本文**への作品別Amazonリンク）は**実質完了**（2026-09-13）
    → 表 3,056本・節ごとの作品ポスター 204本・「他のサービスで探す」2,040本が本文から出ている
    → **地の文の直リンクだけが0本**（`body` 枠）だが、これは
      **記事に生リンクを焼き込まない**という設計がそのまま出たもので、埋める対象ではない
    → 枠ごとの本数は [AFFILIATE.md](./AFFILIATE.md) 1-2

### サイトの改善余地（自覚済み）

- 記事一覧のページネーションは**まだ無い**。トップは 2026-09-13 に
  **12本で打ち切り＋「すべての記事を見る」→ `/archive`** にした（`pages/index.astro` の `LATEST`）。
  記事が増えて `/archive` の月ページが重くなったら、そこで初めて必要になる
- 左の枠のサムネイル画像は **Netflix / Prime Video / Disney+ を配置済み**。
  残るのは `guide.*`（運営方針ページのカード）だけで、いまは仮の汎用画像
  `public/guide-thumb.svg` が出ている。`site/src/assets/services/` に置けば差し替わる
- `/stats`（サービス別見放題の追加・削除一覧）は**一覧・左の枠から外してある**。読者に用途が伝わりにくいため。
  ページは残してあり、常設ページ下部の関連リンクから辿れる（孤立ページにはしない）
- 読者向けの文面に**内部の言葉を出さない**（収集・定点観測・月次記事）。
  2026-08-23 に一度出してしまい、カテゴリページと `/stats` を書き直した
- PR枠のスクロール追従は**ブラウザで目視確認していない**（headless Edge が
  スクロール後を撮影できなかった）。1200px 以上で開いて一度確認すること
- **作品検索の見え方はブラウザ未確認**（2026-08-24 追加）。
  動作自体はビルド済みHTMLを jsdom に読ませて検証済み
  （全角/半角・件数・↑↓・Enter/Escape・スクロール呼び出し）。
  残るのはハイライトとスクロール位置の**見た目**。`/leaving/prime-video` が確認しやすい
- **記事どうし**の関連リンクが未実装（作品ページどうし・人物ページは 2026-08-29〜30 に実装済み）
- 人物ページへの入口は**フッターだけ**（ヘッダーからは 2026-09-05、左の枠からは 2026-09-17 に外した）。
  ★ 下の2行はヘッダーにあったころの記録。
  390px 幅ではメニューが4項目で2行になり、ヘッダーが135pxを占める。
  詰めたくなったら項目名を短くする（`config.ts` の CATEGORY_HUBS の `label`）
- セクション画像を記事に**初めて**挿し込む `npm run sections -- --write` は手動実行
  （ビルドが記事ファイルを書き換えるのを避けたため。画像の生成自体は自動）。
  ただし**あとから画像が取れるようになったときの差し替えは自動**
  （`.github/workflows/images.yml` が毎日 05:43 JST。2026-09-05〜）
- `2026-08-leaving` は古い形式で書かれた記事（3列の表・`###` の小見出し）。
  現行テンプレートは4列を指定している。読めてはいるので放置している

---

## 4. 落とし穴（すべて実際に踏んだもの）

### 収集のタイミング（2026-08-23 に実測して判明）

**API が終了予定を出す猶予はサービスで全く違う。**

| サービス | 中央値 | **最大** |
|---|---|---|
| Netflix | 22日前 | 28日前 |
| Amazon Prime Video | 11日前 | **11日前** |

9月終了分が見えた推移がこれ。

```
8/09  累計  4件
8/14  累計  7件
8/18  累計 13件
8/21  累計 65件   ← 52件が一気に出現
```

**Prime Video は11日前にしか出さない。** 9月1日に一斉終了する30件は8月21日にならないと見えず、
8月18日に記事を作っていたら65件中13件、**8割を取りこぼしていた**。

**運用の結論**

- **月次記事は前月の20〜22日ごろに作る。** 月初の一斉終了を捉えられる最速のタイミング。
  これより早いと大量に取りこぼし、遅いと読者の視聴猶予が減る。
- ただし**それでも月後半の Prime Video 分は取りこぼす**（11日前にしか現れないため）。
  記事本文が「記事作成時点（◯月◯日時点）では◯本」と必ず断っているのはこのため。
  文言を消さないこと。
- 取りこぼしは**常設ページ `/leaving/<サービス>` が埋める**。
  こちらは `collect` のたびに全件が入れ替わるので、月後半に現れた分もすぐ反映される。
- したがって `collect` は**月2回以上**（20日ごろ＋5日ごろ）が要る。

### 公開後に増えた素材は、誰も拾わない（2026-08-31 に判明）

**「名探偵コナン」劇場版のNetflix分29本が、どの月次記事にも載らないまま8月31日を迎えた。**
きっかけは読者からの指摘（U-NEXTの記事にコナンがあるのに、Netflixの同日終了に触れていない）。

起きたことは分単位でこう。

```
8/09 21:28  2026-08-leaving（Netflix・Prime Video 32本）を公開
8/09 21:53  同じ晩の collect が Netflix の「8月31日終了」61本を取り込む
            └ うち29本が「名探偵コナン」劇場版
8/23        leaving をサービス別に分割（スラッグが 2026-08-leaving-netflix に変わる）
8/25        2026-08-leaving-u-next を公開（U-NEXTのコナンは載る。Netflixの話は書けない）
8/30        conan-movies（主題軸）でようやく両社が1本にそろう
8/31        Netflix分61本のうち30本は、月次記事にはついに載らなかった
```

**データの統合には漏れが無かった。** 61本は `data/events/2026-08.jsonl` に入っており、
常設ページ `/leaving/netflix` にも初日から出ていた。漏れたのは**記事の側**だけ。

原因は3つが重なったもので、どれ単体では事故にならない。

1. **公開と収集が同じ晩に25分差で走った。** 品質ゲートの「取りこぼしチェック」
   （`core/verify.ts`）が見るのは**その1回の生成**だけなので、公開後に来た素材は原理的に検査外。
2. **記事タイプの分割でスラッグが変わり、素材が宙に浮いた。**
   `--list` は「未作成 61件」と正しく出していたが、同じ月・同じカテゴリの記事が
   **別スラッグで**存在したため、運用者からは「もう書いた月」に見えた。
3. **サービス軸の記事は他社に触れられない**（README「記事の種類」の決まり。これは正しい）。
   U-NEXTの記事がNetflixの同日終了を書けないのは仕様どおりで、
   **横断は主題軸（`series` / `special`）の仕事**。そこに気づく合図が無かった。

**打った手（2026-08-31）**

- `npm run write -- --list` に**「記事に載っていない素材」**の欄を足した
  （`pipeline/core/coverage.ts`）。公開済み記事の**本文**と素材を突き合わせ、
  どの記事にも出ていない作品を**締切の近い順**に並べる。
  上の表が「記事が有るか無いか」しか見ないのに対し、こちらは**中身**を見る。
  判定は `ArticleType.mentions` を使うので、サービスごとの表記ゆれは記事タイプ側の宣言に従う。
- 常設ページを根拠にしないこと。`/leaving/<サービス>` は `data/events` を直接読むので、
  記事に無い作品もそこには出る。**それを「載っている」と数えたら検査が永久に空振りする。**

**この欄が出たときにやること**

1. その記事タイプで**更新版**を書く（同じスラッグを書き直す）。
2. 同じ作品がサービスをまたいで並んでいるなら、**主題軸で1本立てる**。
   コナンの件で最終的に正解だったのは `conan-movies`（`series`）で、
   それを8月9日の時点で立てられていれば読者の指摘は出ていない。
3. **サービス軸の記事に他社の表を足して解決しないこと。** 軸が壊れる。
   どうしても触れる必要があるときは、他社の作品を並べない**一文の相互参照**にとどめる
   （2026-08-leaving-u-next のコナンの節がその形）。

### データソース

- **TMDB と Watchmode は使えない。** 無料枠が**非商用限定**で、広告・アフィリエイト収入は
  規約上「商用利用」に該当する。商用ライセンスは TMDB $149/月、Watchmode $349/月。
  → Streaming Availability API は無料枠でも商用利用を明示的に許可している唯一の選択肢。
- **このAPIは日本語に対応していない**（`output_language` は en/es/fr/tr/de のみ）。
  タイトルは英語で返る。邦題は **Wikidata（CC0）** から引いている。
- **邦題をLLMに推測させてはいけない。** 邦題は翻訳ではなく配給時に決まる固有名詞。
  `Paul → 宇宙人ポール`、`Ghost → ゴースト/ニューヨークの幻`。
  推測すると外れ、SEOが機能しない。verify で機械的に禁止している。
- **U-NEXT / Hulu / DMM TV はこのAPIに存在しない。** 作品別の配信状況を裏付けるには
  月2.2万円が最低ライン。代わりに「検索リンク方式」で導線だけ作っている。

### 日付

- **開発機はJST、CIとCloudflareはUTC。** `Date` のローカル系ゲッターを使うと
  日付が1日ずれ、「8月3日終了」が「8月4日」になる。実際に一度作り込んで検出した。
  → 日付整形と月別集計は必ず `pipeline/core/datetime.ts` を経由する。

### LLM

- **`temperature` を送ると Claude Opus 5 / Sonnet 5 は 400 を返す。**
  他社に合わせて共通インターフェースに含めると Anthropic 側が壊れる。
  文体の制御はプロンプトで行う。
- **frontmatter をLLMに書かせない。** 日付・出典・カテゴリはデータから機械的に組む。
  LLMが書くのはタイトル・説明文・本文だけ。
- **出力形式はJSONではなく区切り記号方式。** 長いMarkdownをJSON文字列に入れると
  エスケープ事故が起きる。
- **BOM と CRLF は必ず剥がす。** Windowsのエディタや PowerShell の `Out-File` は
  既定でBOMを付ける。付いていると `^TITLE:` が一致せず、内容は正しいのにパースが失敗する。
  `\r` を残すとタイトル末尾に混入して frontmatter が壊れる。
  → `parseArticle()` で正規化済み。他の入力を扱うときも同じ処理を入れること。

### インフラ

- **GitHub は実メールを含む push をブロックする（GH007）。Privateリポジトリでも働く。**
  → noreply アドレス（`<数値ID>+<ユーザー名>@users.noreply.github.com`）を使う。
  数値IDは `https://api.github.com/users/<ユーザー名>` の `id`。
- **ブラウザ認証は時間制限のある環境では通らない**（`ERR_CONNECTION_RESET`）。
  通常のPowerShellウィンドウから `git push` する。
- **Cloudflare の Build output directory は Root directory からの相対パス。**
  Root=`site` なら出力は `dist`（`site/dist` と書くと404になる）。
- **Cloudflare は Pages を Workers に統合中。** 新規作成UIは Workers に誘導される。
  Pages の作成画面は `dash.cloudflare.com/?to=/:account/pages/new/provider/github`。
- Astro 7 は **Node 22.12.0 以上**。`site/.node-version` で固定済み。

### `data/` のマージ衝突（2026-08-25 に実際に踏んだ）

**CI の自動収集（`chore(collect): …`）と手元のコミットがぶつかると、
`data/events/*.jsonl` と `data/ledger.json` が必ず衝突する。**
どちらも追記型なので、**片方を選ぶと反対側の収集結果が消える。**

| ファイル | 片方を選ぶと何が起きるか |
|---|---|
| `data/events/*.jsonl` | 消えた作品が記事に出ない（素材そのもの） |
| `data/ledger.json` | 既出の記録が消え、**同じ変化を再収集して記事が重複する** |

**正解は和集合。** `--ours` も `--theirs` も間違い。

```bash
# 衝突した3方向を取り出して和集合にする（events は行、ledger は配列）
git show :2:data/events/2026-08.jsonl > /tmp/ours.jsonl   # 手元
git show :3:data/events/2026-08.jsonl > /tmp/theirs.jsonl # CI
# ledger.json は seen / usedRankingThemes を union してソートし直す
```

解決したら**必ず突き合わせる**。ここが0でなければ、どちらかを取りこぼしている。

- 重複した変化キー（`service:kind:作品ID`）が 0件
- 台帳にあってイベントに無いもの、その逆が 0件

実測（2026-08-25）: base 1089 / 手元 1812 / CI 1126 → **和集合 1849**。

**そもそも起こさないほうが安い。** 手元で作業を始める前と、コミットの前に
`git pull --rebase` しておく。`collect-unext.yml` は push 前に `git pull --rebase` を
踏むようにしてあるが、`collect.yml` は踏んでいない（race が起きたら失敗する）。

> `.gitattributes` に `merge=union` を入れれば events の衝突は自動で解決できるが、
> **採らなかった。** 手元とCIが同じ変化を拾っていた場合に、
> 重複した行が静かに2本入る。衝突として気づけるほうがまだ安全。

### 更新の通知（2026-08-24 に追加）

- **通知が来ない＝収集も止まっている、とは限らない。** 通知だけが落ちても
  収集結果はコミットされる（ワークフローのコミット手順は `if: always()`）。
  Actions の実行履歴で「収集結果を通知」の手順だけ赤くなっていないか見る。
- **`permissions: issues: write` が無いと通知だけ 403 で落ちる。** 収集は成功するので
  気づきにくい。エラー本文にこの旨を出すようにしてある。
- **どこまで通知したかは `data/notify-state.json`。** 台帳(`ledger.json`)ではない。
  台帳の `seen` は収集時点で書かれるので、通知の未読管理には使えない。
  進めるのは送信に成功したときだけなので、失敗した回は次回にまとめて届く。
- **初回だけは何も送らない。** 記録が無い状態で走らせると収集済み1,000件超が
  1通に流れ込むため、起点だけ記録して黙って終わる。過去ぶんを見たいときは `--all`。
- **`data/api-usage.json` の8月分は過少。** 計測を 2026-08-24 に始めたので、
  それ以前の消費が入っていない。9月分から実数になる。

### その他

- **Wikidata のラベルにゼロ幅文字が混入することがある**（実例: `ブレイキング・コップス2`）。
  スラッグ生成で壊れるので正規化してから保存している。
- **APIレスポンスの `service` はオブジェクト**（文字列ではない）。
  ドキュメントからは読み取れず、実通信で初めて分かった。

### Markdown と画像（2026-08-23 に踏んだもの）

- **日本語では `**強調**` が成立しないことがある。**
  CommonMark は前後の文字種で開閉を判定するため、`〜します。**次の文` のように
  閉じ記号の直前が句点だと閉じられず、`**` がそのまま画面に出る。
  → `〜します**。次の文` と書く。**品質ゲートが自動検出して公開を止める**
  （`pipeline/core/verify.ts` の `unpairedEmphasis`）。
- **`#` で始まる行はそのまま本文として公開される。**
  Markdown にコメント記法は無い。記事にメモを書き残さないこと。
  実際に11行が公開されていた。気づきはテンプレートに書く。
- **SVG の `@font-face` も `FONTCONFIG_FILE` も効かない**（どちらも実測）。
  画像生成でシステムフォントに依存すると、Cloudflare のビルド環境（Linux）で
  日本語が豆腐になる。→ **opentype.js で文字をパスに変換**し、フォントを同梱している。
- **Astro は `src/pages/_*` を無視する。** 検証用ページの名前を `_` で始めると
  ビルドされず、原因が分かりにくい。
- **`widths` に元画像より大きい数値を書くとビルドが落ちる**（`astro:assets`）。

---

## 5. 費用の現状

| 項目 | 月額 |
|---|---|
| ドメイン `mihoudairader.com` | 約125円（年1,500円） |
| Cloudflare Pages | 0円 |
| GitHub（Public） | 0円（**Public リポジトリなので標準ランナーの Actions は無料**。1節のとおりこのリポジトリは Public） |
| Streaming Availability API | 0円（無料枠500req/月に対し設計消費約250） |
| LLM（月15本想定） | 約$1.7（Claude Sonnet 5） |

**実質の固定費はドメイン代だけ。**

---

## 6. 収益化の見通し（下方修正済み）

当初は U-NEXT 等の高単価ASP案件（1件1,000〜1,500円）を主力に見込んでいたが、
**APIがこれらのサービスをカバーしていないため、データで裏付けた導線は作れない。**

**さらに 2026-08-23 の調査で前提がもう一段変わった。**
Disney+ と Apple TV+ は**そもそも提携できない**（前者はクローズド案件、後者は招待制）。
Netflix に至っては公式のアフィリエイトプログラムが存在しない。

現実的な構成:

| 手段 | 位置づけ |
|---|---|
| **U-NEXT / Hulu のASP案件** | **主力**。917〜1,320円/件。検索リンク方式で導線化（LinkSwitchで自動変換） |
| Amazonアソシエイト（Prime Video） | 対象4社で唯一提携できる。24時間クッキーが実体 |
| DMM TV のASP案件 | 単価最高だが**当面見送り**（FANZA同一基盤でAdSense審査にリスク） |
| Disney+ / Apple TV+ | Disney+ は **afb に案件があり審査中**。ただし **Hulu＋Disney+ パックは不合格**（2026-09-19）なので見込みは低い（AFFILIATE 14-2 の F） |
| AdSense | **本数条件（20〜30本）は 29本で満たした**。**2026-09-19 に申請・審査中** |

皮肉だが、**APIに無いから諦めた U-NEXT / Hulu が収益では主力**になる……はずだった。
❌ **U-NEXT は afb の提携審査に落ち、afb では再申請できない**（2026-09-19）。
**いまの主力は Hulu と Amazon で、AdSense が審査中**（AFFILIATE 14節）。
U-NEXT をやるなら **A8.net などの別ASPで受け直す**しかない（実装は残してある）。

**収益化のスピードは当初想定より遅い**と見ておくこと。

> 実装状況・有効化手順・提携状況の一覧・落とし穴は **[AFFILIATE.md](./AFFILIATE.md)** に集約。
> Phase A（実装）は完了し、**2026-09-13 時点で Amazon と LinkSwitch は本番稼働中**
> （枠別トラッキングIDまで分離済み）。**ASP の提携は 2026-09-19 に決着した**
> （afb は Hulu 単体のみ合格。**U-NEXT は不合格で再申請不可**、Disney+ 単体は審査中＝AFFILIATE 14節）。
